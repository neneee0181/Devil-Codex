---
memoc: true
type: state
scope: project-memory
created: 2026-06-21T11:02:34
updated: 2026-07-21T03:01:42+09:00
status: active
tags:
  - memoc
  - memoc/state
---
# Current Project State

- v0.4.4 Antigravity/Bridge progress fix (2026-07-21): Antigravity tool turns may expose only marked complete user-facing `DEVIL_PROGRESS:` sentences; raw reasoning, code, commands, patches, tool payloads, and secrets remain hidden. Turn-end file tracking reconciles committed and dirty-worktree changes, recovers missing turn IDs, preserves late events, retains the final diff after reload, and deduplicates paths. Stock-Bridge native lite SSE now restores streamed reasoning/messages/tool calls before continuation caching when the completion omits `output`; stale `tools.exec_command` guidance is conditionally repaired to the actually exposed `tools.shell_command`, and repeated equivalent future-plan work memos retain only the latest copy. Full build, proxy tests 36/36, renderer tests 6/6, and diff checks pass. Full main is 45/46; only the unchanged Windows diagnostic-log chmod assertion fails (`0o666` vs expected `0o600`). Installed-app E2E remains.

- Persistent Bridge diagnostics (2026-07-19, unreleased): desktop-main and stock-bridge processes now write separate role/PID/session JSONL app and Bridge logs under Electron `userData/diagnostics`. Actual `/stock/v1` traffic carries one request ID across HTTP/WebSocket ingress, upstream retries, provider adapters, Responses translation, terminal state, and the recent-request UI; provider `finishReason` is retained, including a Gemini `475` text + `STOP` regression. Credentials and binary/data payloads are redacted, raw network chunks are hash-only, complete bounded SSE frames are recorded, files rotate at 32 MiB with 7-day/512 MiB cleanup and 0700/0600 permissions, and shutdown performs a bounded proxy stop plus log flush. Rebased onto origin v0.4.2; combined main tests pass 37/37, full build and `git diff --check` pass. Installed-app incident capture remains.

- Antigravity Sites bridge repair (2026-07-19, uncommitted): Gemini tool-schema normalization now preserves user argument names such as `title`, `default`, and `examples` inside every `properties` map while still removing unsupported metadata from the schema nodes themselves. This fixes Sites `create_site(title, slug)` losing its required `title`. External-provider guidance now treats connector results as authoritative, forbids claiming create/update/deploy success without confirmation, and reports missing existing resources instead of substituting localhost or silently creating a replacement unless explicitly authorized. Antigravity responses are buffered until a tool turn is known, then ordinary narration/raw patch text is suppressed while final text-only answers remain intact. Cached plugin versions are selected deterministically by parsed version rather than OS directory order. `npm run test:main` passes 32/32, full `npm run build` passes, and `git diff --check` passes. Installed-app Antigravity + Sites E2E remains; the current Sites connection still returns 404 for the persisted project and an empty site list, which is outside the local bridge path.

- Antigravity custom-tool signature fix (2026-07-18, uncommitted): diagnosed session `019f7526-c472-7522-91cc-9a9398c2e8be` on installed v0.4.0. Eleven ordinary `function_call` steps succeeded because their synthetic `fc_...` item ids were rejected as signatures; the first `custom_tool_call` (`apply_patch`) used `ctc_...`, which both signature validators incorrectly accepted and replayed to Gemini instead of the cached opaque signature, causing deterministic `Corrupted thought signature` failures after the tool result. The parser no longer maps Responses item ids to `thoughtSignature`, both validators reject `ctc_`/`tsc_`, and independent parser/Google-wire/replay regression tests cover the failure path. `npm run test:main` passes 29 tests, full `npm run build` passes, and `git diff --check` passes. Installed-app E2E remains pending; the already-poisoned session cannot self-recover because its replay cache was cleared, so verify in a newly created Antigravity thread after rebuilding/reinstalling.

- Internal provider/Bridge UX hardening (2026-07-18, uncommitted): settings persistence and MCP/Bridge/remote runtime effects now run in a serialized transaction and restore the prior config/runtime on failure; startup Bridge failure safely disables Bridge without blocking the app. Renderer settings share one authoritative controller, show exact cross-platform errors, and reconcile optimistic state. Local Ollama/vLLM/LM Studio accounts become selectable only after a successful current-session model refresh; provider readiness is shared by the internal picker, stock catalog, and proxy discovery. Model-specific reasoning/speed controls, external-turn preparation feedback, and an in-composer Bridge-off action were added. Full build, 26 main tests, and `git diff --check` pass.

- OpenCodex proxy parity completion (2026-07-18, uncommitted): both Devil-internal external-model turns and stock Bridge turns now share the same Responses state/compaction, complete tool catalogs, provider-specific schema/reasoning/image translation, strict stream lifecycle, compressed request handling, retry/error diagnostics, and Antigravity/Anthropic/OpenAI-compatible continuation behavior. Stock discovery is isolated at `/stock/v1`, exposes only the ordered Bridge selection, accepts unambiguous pre-account model IDs, and the settings picker excludes disconnected accounts. Packaged Windows uses its quoted ONLOGON task; packaged macOS uses an escaped LaunchAgent plus normal shutdown handoff. OpenCodex reference commit `2bdeee8f2252eecdc52a681358f7f98a1aad1f31`; `npm run build`, 23 main tests, and `git diff --check` pass.

- OpenCodex response-chain parity (2026-07-18, uncommitted): external Bridge Responses requests now persist completed response `output` plus request `input`, expand `previous_response_id` on the next external request, and reload the state from a bounded 1-hour disk snapshot. Failed/incomplete responses are not stored; shutdown flushes pending state. Build, main test, and in-process/disk-reload smokes pass.

- External tool-catalog nudge parity (2026-07-18, uncommitted): OpenCodex-style compact tool-contract guidance now accompanies the actual wire tool names for Google/Antigravity, Anthropic, and non-OpenAI OpenAI-compatible providers; official OpenAI is excluded and `tool_choice: none` suppresses it. Main build/test and provider-body smoke pass.

- Bridge multi-selection persistence fix (2026-07-18, uncommitted): stock model picker clicks could issue concurrent `settings:save` IPC calls; `CodexSettingsStore` now serializes read-modify-write cycles so a stale shorter selection cannot overwrite the final list. Concurrent 12-selection smoke test and Electron main build pass. Current local config still contains only the previously persisted two models and requires re-selection for models that were never saved.

- Bridge stale-model overwrite fix (2026-07-18, v0.3.18): separate `useCodexSettings()` instances in the main view and Bridge picker could keep different snapshots; the main view could save an old external-model list after the picker removed it, leaving `devil-codex-catalog.json` stale. Added a renderer IPC listener for authoritative `settings:changed` events so all hooks converge on the saved list. Full build, main test, and diff check pass.

- Stock Bridge zstd request fix (2026-07-18, uncommitted): session `019f733f-7b48-7982-a467-69e0f76bfb0d` sent a zstd-compressed Responses request; the proxy parsed compressed bytes as UTF-8 JSON and returned 500, which stock Codex displayed as a generic high-demand error. Proxy request bodies now decode zstd, gzip, deflate, and brotli before parsing. Full build, main test, and diff check pass.
- OpenCodex parity pass (2026-07-18, uncommitted): Bridge now handles `/v1/responses/compact`, bounded compressed bodies, upstream retry/timeout, reasoning envelopes/signatures, allowed tool choices, web-search history, image limits, atomic model catalogs, and Responses WebSocket frames. `npm run build`, `npm run test:main`, and `git diff --check` pass.
- Stock session `019f733f-7b48-7982-a467-69e0f76bfb0d` follow-up (2026-07-18): Codex Desktop WebSocket-lite requests carried all active tools inside `input[]` as `type:"additional_tools"`, with no top-level `tools`. Devil ignored that item, so Antigravity received zero tools and returned an English preamble before ending. Parser now collects and merges `additional_tools` through the normal tool adapter path; compiled parser smoke confirmed custom/function/namespace tools.
- Antigravity schema parity (2026-07-18, uncommitted): Gemini tool schemas now remove Codex's Responses-only `encrypted` marker and other unsupported JSON-Schema keywords, resolve local `$ref` definitions, convert nullable type arrays/const values, and filter invalid required names, matching OpenCodex's Google sanitizer. Reproduced the shown `Unknown name "encrypted"` payload; build, schema smoke, main test, and diff check pass.
- Provider adapter parity (2026-07-18, uncommitted): OpenAI-compatible translation now applies DeepSeek reasoning-history rules, Z.AI/GLM bracketed model stripping, Kimi locked sampling/tool-choice rules, OpenCode Zen schema cleanup, xAI root-schema normalization, and Claude Sonnet 5+ adaptive thinking. Provider wire fixtures pass for DeepSeek, GLM, Kimi, Zen, xAI, and Claude.

- OpenCode Free provider (2026-07-18, uncommitted): added keyless `opencode-free` using `https://opencode.ai/zen/v1`, `x-opencode-client: desktop`, live `/models` discovery filtered to `big-pickle` and `*-free`, and Bridge/main/side model-picker exposure. Added privacy warning because OpenCode documents data-use exceptions for free models. Main/renderer builds, diff check, live endpoint model-list check, and provider config smoke pass.

Last synced: 2026-07-11

## Current Status

- Bridge save-failure fix (2026-07-18, uncommitted): Bridge activation saved config/catalog successfully but its `/v1/models` health check omitted a selected Claude login model because proxy discovery used only live OAuth results. Login proxy rows now union live, connected-account, and provider-cached models, matching the catalog path; TypeScript `--noEmit` and `git diff --check` pass. Packaged-app Bridge re-toggle remains.

- Bridge model-picker/version fix (2026-07-18, uncommitted): stock Codex routed catalog IDs now use OpenCodex-compatible `provider[@account]/model` slugs; proxy `/models` and request parsing accept slash plus legacy colon IDs. Existing Bridge settings migrate on load/save, and connected login/API-key filtering remains enforced. Main/renderer builds, main test, catalog and migration smoke checks pass. Version 0.3.16.

- Stock Codex projectless-thread parity (2026-07-18, uncommitted): native Codex summaries are marked from `.codex-global-state.json` `projectless-thread-ids`; renderer excludes those threads from project folders and shows them in the standalone chat list. Opening one keeps a projectless UI workspace instead of reclassifying it as a project. Electron-hosted TypeScript `--noEmit` and `git diff --check` pass; manual stock-thread/sidebar check remains.

- File-change card timing fix (2026-07-18, uncommitted): `ChangesCard` now stays hidden while the agent's final message is streaming and appears after the turn leaves the running state. Electron-hosted TypeScript `--noEmit` and `git diff --check` pass; manual completion-stream check remains.

- Long-paste live-history fix (2026-07-18, uncommitted): app-server completion sync can echo the model-bound `첨부 파일` context as full user text, while the optimistic renderer has a compact `pasted-text-*.txt` attachment card. `mergeTimelineItems` now matches that overlay to the attachment message and preserves the compact display text. Electron-hosted TypeScript `--noEmit` and `git diff --check` pass; manual long-paste completion/reload check remains.

- Native steering UI flow fix (2026-07-18, uncommitted): direct `turn/steer` marks the old turn interrupted, removes its activity/agent items, suppresses the old completion from recreating that activity, and lets the new `turn/started` create a fresh work tab below the user message. `turn/completed` only clears running/busy state when its turn id is still current. Electron-hosted TypeScript `--noEmit` and `git diff --check` pass; manual live steering remains.

- Stock Bridge follow-up (2026-07-18, uncommitted): Bridge catalog/UI now union account-specific and provider-wide model lists so staggered OAuth/API refreshes do not hide connected models. macOS stock-app relaunch supports both `Codex` and `ChatGPT` app registrations/process names. Electron main TypeScript `--noEmit` and `git diff --check` pass; packaged macOS/manual Bridge toggle remains.

- Stock Bridge follow-up (2026-07-17, uncommitted): session `019f7045-8b50-7121-8ad1-6d5ad6bc43f2` selected Antigravity correctly and reached `127.0.0.1:49873`, but stock Codex first tried WebSocket Responses and received Bridge HTTP `404`; its HTTP fallback then returned `500`, so Antigravity was never reached. Rapid Bridge picker clicks also reused stale renderer selection state, causing later saves to overwrite earlier models; it now commits every operation from a latest-selection ref. OpenCodex source comparison confirmed the built-in OpenAI provider probes WebSocket even when catalog rows omit the capability. Devil Bridge preserves stock OpenAI thread identity via managed `openai_base_url`, removes `supports_websockets` and `prefer_websockets` from external rows, and returns HTTP `426 Upgrade Required` for authenticated `/v1/responses` upgrades so Codex uses the same-session HTTP/SSE fallback. Windows relaunch identifies `OpenAI.Codex_*` by AppID because current Start menu label is `ChatGPT`, and Bridge sync verifies `/v1/models` includes every selected model. `npm run build`, `npm run test:main`, `git diff --check`, config activation/removal, catalog WS gate, and compiled 426 fallback checks pass; installed-app Antigravity E2E remains.
- Subagent activity parity v0.3.4 (2026-07-16): `SideChat` previously filtered a child thread's history down to only user/agent text, dropping `activity` items entirely. It now renders child activity entries with the shared `TurnActivity` component, including expandable searches/commands/MCP inputs and per-file collapsible diffs; file-open actions route to the workspace file panel in both docks. Subagent history is independently keyed by child `threadId`, and its loader calls `readThread` using that exact ID, so it does not reuse the main-chat history. `npm run build`, `npm run test:main`, and `git diff --check` pass; released as v0.3.4.
- MCP readiness v0.3.5 (2026-07-16): Settings now checks actual BrowserControlServer and DesktopControlServer listeners plus managed `devil_browser`/`devil_computer` config registration. It reports ready, disabled, Bridge-disabled, or error rather than treating the preference as proof. Current local install has Bridge OFF, enabled registration, and both named pipes open.
- Browser routing v0.3.6 (2026-07-16): Bridge OFF with Devil browser MCP ON disables only `browser@openai-bundled` while the Devil process owns browser control, because that stock skill is iab-only and blocks external browser MCP usage. The original plugin table is saved and restored on Bridge ON, MCP OFF, or app exit; all other plugins/MCPs remain. Composer detects Devil/우측/내장 browser intent and explicitly selects `devil_browser`; status checks App Server tools as well as config/socket state.
- Activity detail v0.3.3 (2026-07-16): work cards now expose each grouped code search/file read as a separate expandable row, showing the human-readable action plus original command, working directory and output. MCP rows show both input and result; recursive sensitive-key redaction hides API keys, authorization, tokens, secrets, passwords, cookies, and credentials before data reaches the renderer/persisted timeline. File-change rows provide a per-file collapsible diff and file-open control. `npm run build`, `npm run test:main`, and `git diff --check` pass; tag pending.
- Project sidebar cwd-merge fix v0.3.2 (2026-07-16): opening a memoc thread changed `workspace` synchronously while the fast-refresh `threads` list still held Devil-Codex rows. `projectGroups` then merged that stale list into the new workspace unconditionally. It now merges a fast-refresh row only when `cwdKey(summary.cwd)` equals the active workspace key, preserving project isolation even while refresh is pending or fails. Full build and diff check pass; tag pending.
- Bridge Windows launcher fix v0.3.1 (2026-07-16): screenshot investigation showed the Bridge-triggered stock Codex restart falling through to the `Codex` command, which resolved Devil's bundled CLI and opened its directory-trust CMD prompt. `openNativeCodex()` now launches only a `Get-StartApps` entry whose AppID starts `OpenAI.Codex_`; the CLI/cmd fallbacks are removed. If stock Codex is not installed or discoverable, it logs a restart failure instead of opening a terminal. Main build passes; tag pending.
- Release v0.3.0 (2026-07-16): version-only promotion of the completed stock Bridge live-toggle, exclusive MCP/chat mode, and new-chat-entry UX work. No behavior change from v0.2.22.
- Bridge entry UX v0.2.22 (2026-07-16): Bridge ON preserves existing thread/settings/file/terminal tabs but disables every new-main-chat entry point: sidebar button, project-row pen buttons, Windows File menu entries, native new-thread command, and command-palette new-chat/side-chat commands. `newThread`, project creation, and side-chat creation also guard state in case a shortcut or stale handler gets through. Full `npm run build` and diff check pass; main test pending before tag.
- Bridge exclusive mode v0.2.21 (2026-07-16): user chose Bridge ON as a stock-Codex-only mode. `disableDevilExclusiveMcps()` serially removes the shared config blocks for `devil_browser`, `devil_computer`, `devil_ask`, and `devil_subagent`, stops their local services, and resets app-server state. `restoreDevilExclusiveMcps()` restores only persisted opt-ins immediately when Bridge is OFF. The main process rejects new threads, send, queue, and steer while ON; the renderer reflects the setting immediately through the local settings event and renders a locked composer. Full `npm run build` and diff check pass; run the existing main test and installed-app toggle scenario before release.
- Stock Bridge immediate toggle v0.2.20 (2026-07-16): implemented the approved transparent-proxy architecture described in `docs/STOCK_CODEX_BRIDGE_LIVE_TOGGLE.md`. Devil desktop startup now removes a prior headless handoff owner, starts its own proxy, and immediately installs the stock bridge config/catalog when enabled; it no longer requires Devil to exit before the stock picker sees external models. Settings changes now install/remove the managed root bridge immediately, rebuild it for selected-model changes, and force-restart only an already-running stock Codex desktop process. GPT/Codex model requests remain raw, header-preserving pass-through to ChatGPT Codex; only external `provider(:account):model` requests go through adapters. Desktop exit continues to hand proxy ownership to the headless service. Windows uses the Microsoft Store `OpenAI.Codex_*` package path and AppsFolder launcher; macOS/Linux target known GUI process names only and intentionally never kill the `codex` CLI. `npm run test:main`, full `npm run build`, and `git diff --check` pass. Installed-app manual verification remains.
- Localized README product gallery (2026-07-12, uncommitted): added six supplied 2880 × 1800 screenshots under `assets/screenshots/`. The main-chat view is the centered hero below the project heading; the browser, side-chat/terminal, file panel, question modal, and stock-Codex model picker are a labeled gallery. Added matching `README.en.md` and `README.zh-CN.md` documents plus language navigation in all three. `git diff --check` passes; local links/assets and fenced blocks validate in every README.
- Question tool routing fix (2026-07-11, uncommitted): a native `request_user_input` call from a Default-mode session failed because Codex exposes that tool only in Plan mode. Fresh Default-mode turns now receive a directive to use `devil_ask.ask_user` only for genuinely blocking choices, while native Plan-mode turns receive the inverse directive and must not call Devil Ask. Both directives are stripped before transcript persistence. `npm run build` and `git diff --check` pass; desktop restart plus Default/Plan manual confirmation remains.
- Documentation refresh (2026-07-11): README keeps its existing emoji-led visual shell while updating the project explanation from current source/memoc state. It covers the native-vs-proxied model boundary, current Bridge selection behavior, MCP/remote workflow, settings tabs, architecture, and build/package commands; stale provider roster and old automatic five-model claims were removed. No product version change accompanies this documentation-only update.
- Stock Bridge selection v0.2.14 (2026-07-11): `devil_stock_bridge_models` persists an ordered list of external models picked in Settings -> 구성 -> Bridge. The catalog now contains native GPT models first and only those selected external models afterward; no fixed limit applies. Disabling the Bridge removes the managed catalog/proxy so stock Codex exposes no external rows, while preserving the chosen list for later re-enable. Full build and diff check pass.
- Configuration tabs v0.2.13 (2026-07-11): Settings -> 구성 no longer stacks every control in one scroll. Existing controls keep their exact storage/update paths but are grouped into 기본 (app/permissions/environment), 도구 (MCP), 원격, Bridge, and Sidecar tabs; the tab strip collapses to two columns on narrow layouts. Full build and diff check pass. Browser-only Vite preview cannot mount the complete app without Electron preload, so packaged-app visual click-through remains the final manual check.
- Subagent hardening (2026-07-11): OpenCodex GPT-5.6/multi-agent audit confirmed its v2 `spawn_agent` proxy injection cannot be copied directly into Devil's MCP delegation transport. Devil's own delegate path had a real privilege escalation: it always created children with `never` approval and `danger-full-access`. Children now use the persisted Codex permission ceiling (with an explicit note that the MCP protocol does not yet carry parent thread permissions), support an optional low/medium/high/xhigh effort, return `timed_out` distinctly, and reject completed turns with no result text. Verified full build, diff check, and MCP tools-list schema smoke.
- GPT-5.6 native catalog parity (2026-07-10): Devil's bundled `codex-cli 0.142.4` returned only three legacy rows from `model/list` and overwrote the newer shared cache. Ported OpenCodex's catalog-only idea without routing native requests through the proxy: on Devil desktop startup, `syncNativeCodexCatalog()` preserves the pre-server native cache in `devil-codex-native-models-backup.json`, writes a native-only `devil-codex-native-catalog.json`, and registers only root `model_catalog_json`. No `openai_base_url` is set, so Codex-login turns remain direct. On desktop exit, this native catalog is replaced by the existing external-model stock bridge, or removed entirely when the recovery toggle is off. Static/default model data and app-server list fallback include `gpt-5.6-sol`, `gpt-5.6-terra`, and `gpt-5.6-luna`. Verified full build and an isolated app-server/config smoke: all three appear in `model/list`, catalog root is active, `openai_base_url` absent. Version target: v0.2.12.
- Stock Codex external-model bridge (2026-07-10): ported the relevant OpenCodex integration pattern without changing Devil desktop's native Codex route. `src/main/codex-stock-catalog.cts` clones the installed native `models_cache.json` and adds configured external models as `provider:model` (including account id when needed). On Devil desktop exit a `--devil-stock-proxy` Electron service starts, registers a managed `openai_base_url` plus `model_catalog_json`, and proxies only recognized external prefixes; native model requests are forwarded untouched to ChatGPT/Codex. On next Devil desktop start the managed root bridge is removed before its app-server connects, preserving Codex direct routing. Provider login/key/model changes rebuild the catalog. Verified: `npm run build`, `git diff --check`, temp CODEX_HOME activation/removal, actual bridge HTTP `/v1/models` (440 external rows), actual Codex app-server `model/list` (194 routed rows under its 200-item cap plus `gpt-5.5`), a real `copilot@181058817:gpt-4o` SSE completion, and a real native `gpt-5.5` SSE completion through transparent passthrough.
- Stock bridge feature follow-up (2026-07-10): the selected external provider's first five models now receive catalog priorities `0..4`, ahead of native entries at `20+`, which makes them the candidate set exposed by stock Codex `spawn_agent`; those child turns use the same external proxy. Added Settings -> 구성 -> 순정 Codex Bridge Sidecar opt-ins for external-model web-search and vision support. Both are off by default. Forced Copilot web-search E2E completed with 3 sidecar searches/tool calls; real `build/icon.png` vision E2E completed with 1 request and no sidecar failure. OpenCodex comparison confirms Bridge-critical model injection, native passthrough, subagent candidate priority, sidecars, request diagnostics, and Windows background startup are covered. Its additional specialized provider/OAuth adapters remain deliberately unadvertised until their real transports are implemented.
- Stock bridge recovery toggle (2026-07-10): added Settings -> 구성 -> 순정 Codex Bridge -> `순정 Codex에서 외부 모델 사용`. Default is on to preserve v0.2.10 behavior. Turning it off removes the managed stock bridge config block, deletes the Windows `Devil Codex Stock Bridge` scheduled task, and stops stale `--devil-stock-proxy` processes while leaving Devil desktop's own external-model support intact. Headless bridge startup and desktop quit handoff now respect this setting. Version target: v0.2.11.
- Limitation confirmed against raw app-server `model/list`: native and routed catalog entries expose the same fields and no per-model provider/transport selector. Therefore one stock dropdown cannot truly bypass the local transport for native models while routing only external rows without changing upstream Codex. The implemented bridge matches OpenCodex: native requests are authenticated and forwarded unchanged; Devil desktop mode still removes the bridge and stays proxy-free for native Codex turns.
- Upstream source confirmation: shallow `openai/codex` checkout shows `modelProvider` is selected by thread request/config, whereas catalog rows exposed to the stock picker have no provider mapping. A literal per-model native transport bypass requires an upstream desktop/client change, not another config/catalog field.
- v0.2.8 prep / NVIDIA NIM rate-limit fix (2026-07-09): investigated session `019f4559-db99-74b2-b950-cf945709bb75`; request-log showed repeated NVIDIA `z-ai/glm-5.2` 429 `Too Many Requests` after prior successful calls, while the thread itself peaked at 6 requests/minute, so the failure is consistent with NVIDIA hosted endpoint/account/model dynamic rate limiting rather than this single thread exceeding 40 RPM. Added Settings -> 구성 -> `NVIDIA NIM RPM 제한` (default 40, 0 disables) and a proxy-level NVIDIA upstream pacing queue that applies to normal turns and sidecar loop reinvokes. Provider diagnostics now include `provider.nvidiaRateLimitRpm`. `npm run build` and `git diff --check` pass.
- v0.2.7 prep (2026-07-09): investigated session `019f4509-94b4-7a11-b885-cf680eb88528`. Devil transcript meta had `provider:"copilot"`/`model:"gpt-5.5"`, but Codex rollout `session_meta.model_provider` was `openai` and diagnostics showed `provider: codex`, proving the first external-provider turn raced into Codex direct before `model_provider` was patched to `devil`. Fix: app-server thread start/resume now sends both `modelProvider` and `model_provider`, and external turns wait briefly for the new thread row/rollout before patching/restarting/resuming through the local proxy. Copilot now always uses `/chat/completions` like opencodex's `github-copilot` `openai-chat` adapter, removing the model-name-based `/responses` branch. Antigravity gained opencodex alias models and resolves aliases to CCA wire ids. Added opencodex-style `zai` provider for Z.AI GLM Coding Plan (`glm-5.2`, `glm-5.2[1m]`, `glm-5.1`, `glm-5`, `glm-4.6`) via OpenAI-compatible chat API with `ZAI_API_KEY`/`Z_AI_API_KEY`/`GLM_API_KEY`. Steering queue now remembers the interrupted turn id and inserts the steered user bubble/model notice immediately after that turn instead of appending at the end. `npm run build` passes.
- Antigravity continuity fix (2026-07-08): investigated session `019f40e3-dc2a-73f2-83d6-6849e7a6b2e0`. Request log showed `gpt-oss-120b-medium` failed upstream with `No capacity available for model gpt-oss-120b-medium on the server`; `gemini-3.5-flash-extra-low` produced the first tool call, then follow-up requests failed with `Function call is missing a thought_signature in functionCall parts` for `default_api:shell_command`. Root cause: `streamGoogle` converted Gemini `functionCall` parts into generic tool-call events and dropped `thoughtSignature`; `googleContents` then replayed assistant tool calls without the signature Gemini requires for tool continuity. Fix: `AdapterEvent.tool_call_start`/`OcxToolCall` now carry optional `thoughtSignature`; `streamGoogle` captures camel/snake-case signatures from the returned part; `googleContents` reattaches it to replayed `functionCall` parts; `proxy-server.cts` stores Antigravity signatures by `threadId` + `call_id`, persists them under app `userData/providers/antigravity-tool-signatures.json`, and applies them to later parsed history. Version bumped to `0.2.6`. Verified `npm run build:main` and full `npm run build` pass. Manual E2E still needed in a new Antigravity Gemini thread; old failed session may not recover if its original signature was already lost before this fix.
- v0.2.1 prep (2026-07-07): fixed composer paste undo behavior. Root cause: `insertPlainTextAtSelection()` always used manual Range DOM mutation for pasted text, which bypassed/fragmented the browser contenteditable undo stack. It now tries `document.execCommand("insertText", false, text)` first so Ctrl/Cmd+Z removes the pasted text as a normal editor undo step, with the old Range insertion kept as fallback. Version bumped to `0.2.1`. Verified `npm run build` and `git diff --check`.
- v0.2.0 prep (2026-07-07): remote web connectivity/UX pass. Diagnosis: current Mac's Funnel config is valid (`https://neneee-macbookpro.tail98f2d4.ts.net` -> `http://127.0.0.1:49882`) and local curl to public `/healthz` returns 200, so the phone "network server not found" is likely a public Funnel/DNS reachability path issue rather than missing local static server. Fix: remote server now binds `0.0.0.0` and exposes an additional tokenized direct tailnet URL/QR (`http://100.x.x.x:49882/#t=...`) alongside public Funnel, giving phones with Tailscale enabled a DNS/Funnel fallback. Sanitized remote status now strips both public and tailnet token URLs before broadcasting to remote clients. Settings remote UI now shows two access cards (public Funnel + Tailscale direct), cleaner QR/copy controls, corrected allowlist wording, and styled allowed-thread selection cards instead of raw checkboxes. Version bumped to `0.2.0`. Verified `npm run build`, `git diff --check`, and public Funnel curl/healthz before the source change.
- v0.1.71 prep (2026-07-07): remote web is now strictly allowed-thread-only. Empty `remoteAllowedThreadIds` no longer means unrestricted remote project browsing; it means the remote client can connect but sees no threads and cannot create/read/send until the desktop Settings -> 원격 제어 -> 허용 스레드 includes a thread. `remote:scope` always reports restricted for remote clients, list/search/project channels are always filtered by the allowlist, `thread:create` is denied remotely, and the mobile app defaults to the allowed-thread UI so project/new-thread controls do not flash while scope loads. Verified `npm run build` and `git diff --check`.
- v0.1.70 prep (2026-07-07): fixed remote Tailscale status crash where `tailscale status --json` sometimes returned plain text beginning with "The Tailsc..." and the UI surfaced `Unexpected token 'T'... is not valid JSON`. `TailscaleCli.status()` now treats non-JSON status output as an installed/offline Tailscale error message instead of a parser failure. Also adjusted the floating environment card higher with a smaller max-height so long usage/source lists scroll inside the card and do not run into the composer fade. Verified `npm run build`, `git diff --check`, and restricted-PATH Tailscale status smoke (`installed:true`, `online:true`, app bundle CLI).
- v0.1.69 prep (2026-07-07): fixed macOS Tailscale CLI detection for remote Funnel. Root cause: `TailscaleCli.detectCli()` only tried `tailscale` from `PATH`; Finder-launched Electron apps often miss `/usr/local/bin`/Homebrew paths even when Tailscale is installed. macOS now checks the Tailscale app bundle CLI (`/Applications/Tailscale.app/Contents/MacOS/tailscale`/`Tailscale`) plus common bin paths before reporting missing CLI; Linux also has common absolute fallbacks. Version bumped to `0.1.69`. Verified `npm run build:main`, `npm run build`, and a restricted-PATH Node smoke where status resolves the app bundle CLI and reports installed/online.
- Usage/cache stability follow-up (2026-07-07): after investigating Claude session `d051aa52-b1a8-4915-8cba-ee762c1023f6`, thread/settings usage UI now separates fresh tokens (`uncached input + cache creation + output`) from throughput/cache reuse/creation, preserves cache fields across Codex and Claude history reloads, and surfaces Claude `cache_miss_reason` diagnostics as timeline/settings/environment warnings. "최종 응답 본문 누락" rows now use the active runtime label, merge live+synced history before fallback checks, and remove stale warnings when an agent answer exists. Model-change notices persist via cached thread history. Claude MCP config no longer changes per selected browser/computer skill, ask-user guidance is no longer appended to every turn's model-bound text, and busy follow-up messages always show the queued edit/steer/cancel UI before steering. `PATH=/opt/homebrew/bin:$PATH npm run build` passes after restoring lockfile deps with `npm install`.
- v0.1.45 prep (2026-07-06): fixed markdown file-link handling in chat/file previews. Root cause for session `019f3598-6e76-77c2-b333-06a970373838`: `MarkdownContent` auto-linked `.memoc/...` starting at `memoc/...`, leaving a literal leading dot outside the link and producing unnatural `.[memoc/...](devil-file:memoc%2F...)` display; file clicks also passed `:line[:col]` suffixes such as `.memoc/wiki/project/remote-control.md:16` through to `workspace:read-file`, so the file panel searched for a non-existent filename. `MarkdownContent` now normalizes `devil-file:` paths, strips line/column suffixes, repairs old malformed `. [memoc]`-style links, and includes `.memoc/...` inside generated links. `WorkspaceFilesPanel` normalizes both target and nested markdown open paths before `findWorkspaceFile`/`readWorkspaceFile`. Version bumped to `0.1.45`; `npm run build:main`, `npm run build:renderer`, and `git diff --check` pass.
- Remote-control MVP (2026-07-06): implemented phone/browser remote control per wiki direction. New main modules: `remote-server.cts` (HTTP static `dist-mobile/`, `/healthz`, `/ws` JSON IPC bridge, event broadcast, heartbeat, channel/event allowlists), `remote-auth.cts` (safeStorage/plain token store, approved devices, token regeneration invalidates devices, IP auth rate limit), `tailscale.cts` (CLI status/cert/funnel + 100.64/10 detection). `main.cts` now exposes only core remote channels through `ipcHandlers`, broadcasts allowed events from `sendToRenderer`, adds `remote:*` local IPC, starts saved remote settings on boot, stops on quit, and supports tailnet bind + Funnel opt-in. `SettingsView` adds 구성 -> 원격 제어 URL/QR/device/client/status controls. Added `src/mobile` PWA for project/thread list, chat/read/send/interrupt, approval/ask responses, and usage. Build chain adds `build:mobile`, `dist-mobile/**`, deps `ws`/`qrcode`. Verified `npm run build`; real phone/Funnel manual E2E still pending.
- Remote-control plan (2026-07-06, docs only, NOT implemented): user approved building phone/browser remote control for Devil. Decisions: default Tailscale tailnet-only, opt-in Tailscale Funnel public URL (requires token auth + device approval + rate limit), mobile web UI covers core only (thread list, chat, send turn, approval respond, usage), no native app (PWA). Full step-by-step implementation spec for another agent lives in `.memoc/wiki/project/remote-control.md` (IPC handler-registry refactor in `main.cts`, new `remote-server.cts`/`remote-auth.cts`/`tailscale.cts`, `src/mobile/` web app + `vite.mobile.config.ts`, SettingsView section, channel whitelist, WS protocol, test plan). Also released v0.1.41 (tag pushed; release workflow builds installers) containing the token-audit fixes.
- v0.1.43 prep (2026-07-05): fixed terminal startup crash affecting both right and bottom terminal tabs, especially when opened from a project new thread. Root cause: terminal `auto` preferred WSL; when PTY spawn failed, fallback `child_process.spawn()` retried the same shell without an `error` listener, so `wsl.exe ENOENT` surfaced as an uncaught main-process JS error. `TerminalManager` now resolves ordered shell candidates (WSL -> Git Bash -> pwsh -> PowerShell -> cmd), tries them for PTY, falls back to cmd for compatibility mode, catches fallback spawn errors, guards writes to closed stdin, and opens from `process.cwd()` with a visible warning if the requested project cwd is missing. Version bumped to `0.1.43`. `npm run build:main`, `npm run build:renderer`, and `git diff --check` pass. Direct Node smoke created sessions for missing/normal cwd but ConPTY helper did not exit cleanly outside Electron, so final UX check is installed-app side.
- v0.1.42 prep (2026-07-05): fixed misleading Codex quota usage in Settings/account usage. Live `wham/usage` response showed official `rate_limit.primary_window.used_percent`/`secondary_window.used_percent`; parser now prefers those known 5h/7d windows and only falls back to generic recursive parsing if absent, preventing monthly/additional quota candidates from mixing into the 5h row and showing false 100% used. Version bumped to `0.1.42`. Verified `npm run build:main`, `npm run build:renderer`, fixture with monthly=100 still returning `5시간 used=0 remaining=100`, and live Codex smoke returning low 5h usage instead of 100%.
- Codex-mode token audit (2026-07-05, commit `7ef7cd6`): verified Codex mode has NO per-turn respawn problem — one `codex app-server` process per thread (`threadServers` LRU, `MAX_THREAD_APP_SERVERS`), turns reuse the loaded thread. Rollout analysis: Devil-driven native gpt-5.5 multi-turn session (41MB, 112 turns, 1,674 calls) cached 91.6% vs stock codex-tui 92.6% — parity, no leak. All "zero-cache" anomalies in Devil rollouts were proxy-routed external-provider turns (DeepSeek/antigravity/copilot), not native codex: the OpenAI-compatible proxy adapters dropped `prompt_tokens_details.cached_tokens` / DeepSeek `prompt_cache_hit_tokens`, so proxied turns displayed cached=0 and cost summaries priced the full prompt at the uncached rate. Fixed in `api-key.cts` (`openAiCompatibleUsage`) + `copilot.cts`. `externalThreadTitle` confirmed local-only (no hidden model call). tsc(electron) + build pass.
- Persistent Claude Code process per thread (2026-07-05, commit `291f7b8`): `ClaudeCodeRuntime` now keeps one live SDK `query()` per Devil thread (`ThreadInstance` map, streaming-input generator fed by `push`); later turns stream into the same process instead of respawn+resume, so SessionStart hooks fire once per session (not per turn) and the prompt-cache prefix stays warm. Model change → `request.setModel`; permission-mode/mcpConfig/dialog-config change (instance fingerprint), user stop (`interruptTurn` disposes → catch path still reports "interrupted"), pump error, and 10-min idle (`CLAUDE_INSTANCE_IDLE_MS`, past 5-min cache TTL) → dispose; next turn resumes the same native session in a fresh process. `canUseToolBridge` takes a `() => turnId` getter; `main.cts` `before-quit` calls `disposeAllInstances()`. Smoke test (built `dist-electron/claude-runtime.cjs`, 2 haiku turns, same thread): one SessionStart hook payload in JSONL, same session id, turn 2 usage `cache_read 16,896 / cache_creation 234`, turn latency 5.8s → 2.3s. Kept as-is by user decision: per-turn Ask-user/English directives and devil_ask/devil_subagent MCP tool defs (intentional features, small cost). tsc(electron) + build pass.
- Claude-mode token usage investigation + accounting fix (2026-07-05): user reported Claude Code mode "uses far more tokens than stock CLI". Session JSONL analysis (`64616d2f`, `bc5219cd`, fork chain `c00f10f9→e8dd8e2c`) showed actual API usage is at parity — prompt cache reads grow normally across per-turn SDK respawns (`cc` small, `cr` growing, `in≈2`/call); the only real full-context rebuilds were harness-inherent (ToolSearch deferred-tool load invalidating the tool-definition prefix, >5min idle cache TTL — both identical in stock CLI). Root cause of the perceived gap: Devil summed `cache_read + cache_creation` into `cached_input_tokens` and counted it at full weight in "전체 토큰" (e.g. one agentic fable turn displayed 6.5M tokens where raw input was 9.2k/output 48k). Fix: `claude-runtime.cts` now reports `cache_read_input_tokens`/`cache_creation_input_tokens` separately through `ClaudeTurnUsage`/`ProviderTokenUsage`; `providerPricing.ts` prices cache writes at 1.25x input and reads at the cached rate; usage UI headline is now "실사용 토큰" (uncached input + cache creation + output, comparable to CLI /cost) with "전체 처리량"/"캐시 재사용"/"캐시 생성" shown separately (SettingsView, thread usage rows, threadTimeline passthrough). Remaining known real (small) per-turn overheads, not yet changed: Ask-user/English directives appended to every model-bound turn, devil_ask/devil_subagent MCP tool defs, SessionStart hooks re-firing on each per-turn SDK resume. `npx tsc --noEmit` (both tsconfigs), `npm run build` pass; committed `6690e01`.
- Post-v0.1.39 local fix (2026-07-05): session `64616d2f-677e-4879-b6ac-887bed604431` showed the internal Ask-user MCP directive leaking into visible chat because Claude Code native continuation summaries and model-bound directive blocks were imported as ordinary user text. `main.cts` now strips robust Ask-user directive blocks and hides internal "This session is being continued..." summary user rows on history reads; `provider-transcript.cts` skips those rows during Claude JSONL import. Verified against the stored session (directive removed, item count 9→8), `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check`.
- Claude-mode delegate_subagent parity (2026-07-05, post-v0.1.38): verified Codex-mode delegate flow is correct (session `019f2e0b`, deepseek child), then fixed Claude Code parent-mode gaps. (1) Claude Agent SDK prefixes MCP tools (`mcp__devil_subagent__delegate_subagent`), so subagent conversion now suffix-matches in `threadTimeline.ts`/`thread-history.cts` and `history-cache.cts` title normalization; `normalizeCachedDelegateSubagents` is exported and applied to `providerTranscripts` reads in `thread:read`/`thread:sync-history`. (2) Subagent tabs derived runtime from the parent thread; `SideChat` now derives runtime/account from the locked spawn pick (`claude-code` → claude-code, else codex) so a Claude-mode parent reads/continues a Codex-runtime child correctly. (3) `delegateSubagentFromMcp` claude branch now pins `nativeSessionId` to the thread id, persists user/agent transcript, and saves archived meta with `claudeSessionId` so the child tab shows history, resumes, and stays out of the main sidebar. (4) Claude JSONL import (`historyFromClaudeLines`) now pairs `tool_use` with `tool_result` text and rebuilds delegate calls as subagent activities so cards survive reimport. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, `git diff --check` pass. Includes the previous session's uncommitted subagent model-lock work; committed together.
- v0.1.35 prep: Ask-user MCP is now controlled by `ask_user_mcp_enabled` (default true). Settings → 구성 → Devil MCP 도구 exposes "AI 질문 모달 MCP"; Codex config registration and Claude `mcpConfig` include `devil_ask` only when enabled; enabled turns inject a model-only directive to call `devil_ask.ask_user` for genuinely ambiguous branch/trade-off decisions (architecture/data flow/security/cost/dependency/UX/deploy) and to continue with assumptions when context/defaults are sufficient. Verified with `tsc -p tsconfig.json --noEmit`, `tsc -p tsconfig.electron.json`, `vite build`, and `git diff --check`.
- v0.1.35 prep: side-chat/subagent tabs now default to auto model selection. `ToolContent` ranks available provider/account models by tool capability, diagnostics, provider reliability, and prompt text, shows an `자동` badge, and retries recommended fallback candidates before the current model. Manual model picker choices set `auto:false`; the picker includes `자동 추천` to re-enable fallback. `PATH="/opt/homebrew/bin:/usr/local/bin:$PATH" ./node_modules/.bin/tsc -p tsconfig.json --noEmit`, `PATH="/opt/homebrew/bin:/usr/local/bin:$PATH" ./node_modules/.bin/vite build`, and `git diff --check` pass locally.
- v0.1.35 prep: investigated session `019f2c07-e03f-7661-8424-08eda3694721`. Refresh-then-stop failed because renderer lost `activeTurnsByThread` and sent Codex app-server `turn/interrupt` without `turnId`; renderer now recovers the latest running activity turnId from visible/cache history and app-server refuses missing turnId locally as `no active turn to interrupt` instead of forwarding an invalid request. Steering duplicate rows came from native rollout `response_item` + `event_msg user_message` pairs plus history-cache user merge keys including attachment count; user merge key now ignores attachment count after stripping `첨부 파일:` footer so optimistic/native rows coalesce. `PATH="/opt/homebrew/bin:$PATH" ./node_modules/.bin/tsc -p tsconfig.json --noEmit`, `PATH="/opt/homebrew/bin:$PATH" ./node_modules/.bin/vite build`, and `git diff --check` pass locally. Version/tag/push pending.
- Claude Code auto-compact threshold UI follow-up (2026-07-04, v0.1.32 prep): confirmed `d88074f1-e828-4452-9e96-4d02849e6606` only had manual compaction (`trigger:"manual"`). SDK `getContextUsage()` exposes `autoCompactThreshold` and `isAutoCompactEnabled`; `ContextUsage` now preserves those values through live SDK parsing and history reload. Renderer context status/model picker/`/status` use auto-compact threshold as the current-context limit and show raw `maxTokens` separately, so Claude Code current context no longer looks like it should have auto-compacted solely because the raw window/cache-inclusive display is large. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Commit/tag/push in progress.
- Codex effort/serviceTier sync follow-up (2026-07-04, v0.1.31 prep): session `d88074f1-e828-4452-9e96-4d02849e6606` confirmed the root cause: Codex `turn/start` was sending `reasoning:{effort}` even though app-server expects top-level `effort`, so rollout `reasoning_effort` stayed null. `app-server.cts` now sends top-level `effort` and explicit `serviceTier` (`fast` -> `priority`, `standard` -> `default`) so a fast turn cannot leave the thread stuck on priority. `CodexSettingsStore` now reads/writes shared `model_reasoning_effort`/`service_tier`, preserving custom non-priority tiers when unrelated settings save; renderer adopts config values on boot. Native Claude API-limit synthetic messages now import as failed system rows instead of normal agent replies. Session check: manual compaction was present (`preTokens 90,184` -> `postTokens 7,159`), and the final failure was a Claude 429 session-limit error, not a code exception. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, `git diff --check`, and settings round-trip pass locally. Commit/tag/push in progress.
- Claude Code context/auto-compact parity follow-up (2026-07-04, v0.1.30): SDK turns now pass `settings: { autoCompactEnabled: true }` so Claude Code uses native auto-compaction behavior, capture `Query.getContextUsage()` while the SDK query is still alive, and persist that `current-context` usage on the completed Claude Code transcript row. `cache_read_input_tokens`/`cache_creation_input_tokens` result usage is retained for cost/request usage but marked as `scope:"last-request"` + `includesCache`, so the UI no longer treats `1.53M/1.00M` cache-inclusive request usage as current context pressure. Renderer labels/status text now distinguish current SDK context, renderer estimates, and last-request usage. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Commit/tag/push completed.
- Codex slash-menu dynamic-state follow-up (2026-07-04, v0.1.29 prep): confirmed Codex app-server generated protocol exposes `skills/list`, `mcpServerStatus/list`, and `model/list` but no `slash/list` or `command/list`. Renderer therefore keeps built-in slash commands as local fallback while deriving visible detail/availability from live app state: selected model/model count, MCP server/tool counts, enabled skill count, current context usage, response/approval state, and whether an active thread exists for compact/fork. Claude Code SDK slash-command work and Codex duplicate-row normalization remain included. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally.
- Codex interrupted-turn duplicate user row follow-up (2026-07-04): session `019f2a64-1e06-7d00-9342-bee16b9edb7a` was inspected against native rollout `~/.codex/sessions/2026/07/04/rollout-2026-07-04T08-50-32-019f2a64-1e06-7d00-9342-bee16b9edb7a.jsonl` and Devil history cache. Native rollout had one `/` request then `turn_aborted`, followed by the steering request. Devil cache contained both the native user item (`item-275`, with `첨부 파일:` footer/turnId) and the optimistic renderer user item (UUID, same text/attachment, no turnId). Renderer and main history-cache user merge keys now strip the trailing `첨부 파일:` block before comparing, so interrupted/attached turns merge the native and optimistic user rows instead of showing duplicates. Verified the actual duplicate pairs now normalize to identical keys; `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally.
- Claude Code slash-command follow-up (2026-07-04): root cause confirmed in `ComposerSuggestions.tsx`; Claude Code mode was still filtering Devil/Codex's static slash-command array and mixing MCP/skill pseudo-items. `ClaudeCodeRuntime.listSlashCommands()` now initializes the same Claude Agent SDK executable used for turns and reads `supportedCommands()` for the active cwd/model. Main/preload/contracts expose `listClaudeSlashCommands`, renderer loads it only in Claude Code mode, and the Composer shows those SDK commands for `/` while inserting literal `/command ` text so Claude Code handles the command natively. Local SDK smoke check returned 87 commands for this repo, including `vercel:*`, `codex:*`, and `memoc-*`. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally.
- Claude Code context/compact/duplicate-render follow-up (2026-07-04, v0.1.28 prep): root cause confirmed in code and native JSONL. `claude-runtime.cts` had been adding `cache_read_input_tokens`/`cache_creation_input_tokens` into `input_tokens`, then renderer interpreted that same usage as current context, causing misleading `입력 ... · 캐시 ...` double display and context pressure ambiguity. Runtime usage now stores raw uncached input separately, sends prompt-pressure as explicit `contextUsage`, and request/cost summaries normalize old cache-in-input logs and new separated logs. Claude SDK `system/compact_boundary` messages are now rendered as compaction activities live, and `ProviderTranscriptStore` imports native Claude `compact_boundary` records so automatic/manual compaction survives restart. Duplicate Claude Code response display was traced to active-thread periodic sync merging live SDK timeline items (`claude-${uuid}` turn ids) with native JSONL import items (`${threadId}-claude-turn-n` turn ids); active Claude Code threads now skip that periodic merge, and completed transcript append includes the live `turnId` to avoid turnless final-answer duplicates. Verified d051 native JSONL/transcript stored the cited messages once each. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Commit/push in progress.
- Back/forward navigation follow-up (2026-07-04): `NavigationEntry` now stores the active runtime, snapshots use `threadRef`/`itemsRef`, restore switches runtime with `keepThreadOnRuntimeSwitch` instead of letting the runtime-switch effect clear the thread, and nav button enabled state is backed by React state instead of reading ref lengths directly. This keeps Codex/Claude threads attached when using app back/forward buttons. Local validation still passes.
- Post-v0.1.26 local release prep (2026-07-04): pulled `origin/main`, installed newly locked Claude SDK deps, and rebuilt ignored `dist/` + `dist-electron/`. Provider quota bug root cause was stale built `dist-electron/provider-usage.cjs` running old percent logic while source already had remaining-percent parsing; built smoke check against `~/.codex/auth.json` returned current Codex `5시간 used=26 remaining=74`, `7일 used=100 remaining=0`. Mac lag fixes: composer input uses cheaper single-pass snapshots/no-clone reads and 350ms draft-save debounce; thread list history prefetch waits for idle and only loads top 2 uncached histories; thread scroll no longer React-updates/storage-writes on every tiny scroll movement and the full timeline no longer forces a promoted GPU layer. Runtime-switch slowness root cause was measured: Claude import parsed 259.5MB of `~/.claude/projects` JSONL on every `summaries()`/`read()` (~1.8s). `ProviderTranscriptStore` now keeps an in-memory store cache and skips unchanged Claude JSONLs by mtime/size (~2ms stat path). Follow-up perf pass skips no-op thread/project polling state updates and memoizes timeline/Markdown/activity rendering so unrelated App state changes do not reparse old messages. Bottom dock state keys include runtime plus `new-chat`/`project-draft` cwd scope when no thread id exists, and UI-state moves handle runtime-prefixed keys. `package.json`/`package-lock.json` bumped to `0.1.26`; `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Commit/tag/push in progress.
- Post-v0.1.15 local hotfix (2026-07-03): right/bottom review tabs now auto-refresh workspace changes and the selected diff while visible, polling quietly at ~0.9s during active AI work and ~1.8s while idle. Terminal command-history cleanup now preserves CR/CRLF as newlines and applies backspace/delete edits so Windows `cmd dir` output no longer collapses lines or shows input edit artifacts like `c\b \bcls`. Devil-created Claude Code JSONLs were recorded as `entrypoint:"sdk-cli"` while stock CLI sessions use `entrypoint:"cli"`; new sends now normalize the just-created native session JSONL back to `cli` for resume-list compatibility. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Post-v0.1.14 local hotfix (2026-07-02): user decided `CAVEMAN MODE ACTIVE` hook-only sessions should remain because stock Claude CLI shows them too; previously moved hook-only JSONLs were restored to `~/.claude/projects`, and code no longer quarantines hook-only sessions or rewrites Claude JSONL `entrypoint`. Composer clear now uses `replaceChildren()` and resets per draft key, uncached thread opens clear initializing state after navigation so first-run loading UI is visible, visible chat timeline + terminal command-history output updates are batched with `requestAnimationFrame`, and streaming agent text renders lightweight pre-wrap text until turn completion before switching back to Markdown. Per-turn change metadata is precomputed to reduce rerender work while streaming. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Post-v0.1.13 local hotfix (2026-07-02): fixed keyed terminal reuse by wrapping `node-pty` methods explicitly before storing records, so thread switches no longer hit `existing.resize is not a function`. Claude Code JSONL import now keeps every `~/.claude/projects/**/*.jsonl` CLI session that has no visible user/assistant messages by creating metadata from hook/attachment text and deriving missing cwd from the Claude project folder key; future Devil-created Claude sessions set `CLAUDE_CODE_ENTRYPOINT=cli` to better match stock Claude Code session pickers. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Claude Code dynamic skills follow-up (2026-07-02): main exposes `claude:skills`, scanning `~/.claude/skills/*/SKILL.md` frontmatter into `CodexSkillInfo`. Renderer loads those once, merges them with `browser-use`/`computer-use` for Claude Code composer mode, and selected dynamic Claude skills add a prompt directive telling Claude to read the selected `SKILL.md` path before using it. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Claude Code native transcript sync follow-up (2026-07-02): `ProviderTranscriptStore` now imports `~/.claude/projects/**/*.jsonl` sessions into Devil's Claude Code sidebar/history, mapping existing Devil threads by `claudeSessionId` when possible. Manual `/compact` now persists a visible compaction marker after `compactThread` completes. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Claude Code create parity follow-up (2026-07-02): new Claude Code threads now reserve the Devil thread UUID as `claudeSessionId`; first send passes it as SDK `sessionId` when no native transcript exists, then uses `resume` once `getSessionInfo` finds the native session. Renderer receives the reserved `claudeSessionId` immediately. Validation passes locally. Not committed/pushed/versioned yet.
- Turn activity status follow-up (2026-07-02): fixed a stale "응답 작성 중" card after a completed response by finalizing nested `inProgress` activity entries on `turn/completed`. Validation passes locally. Not committed/pushed/versioned yet.
- Claude JSONL import cleanup follow-up (2026-07-02): native Claude Code imports now filter CLI local-command records (`/model` caveats/stdout), skip tool-result-only user records, and import completed native sessions as clean user/assistant text instead of noisy 0s activity cards. Existing noisy imports are replaced on next import. Validation passes locally. Not committed/pushed/versioned yet.
- Terminal persistence follow-up (2026-07-02): right and bottom dock terminals now pass per-thread/per-dock keys into `TerminalManager`; keyed PTYs stay alive across thread/tab remounts and replay buffered output into xterm on return. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Packaged Claude binary follow-up (2026-07-02): Claude SDK turns now pass `pathToClaudeCodeExecutable` from `DEVIL_CLAUDE_BIN` or the bundled executable resolved through `app.asar.unpacked`, avoiding packaged Windows failures where the SDK tried to spawn `app.asar` paths. Verified installed unpacked `claude.exe --version` returns `2.1.198`. Validation passes locally. Not committed/pushed/versioned yet.
- MCP slash mention follow-up (2026-07-02): composer now loads connected MCP servers via `listMcpServers({threadId})` and includes them in `/` suggestions for Codex and Claude Code composer modes. Selecting `/server` stores an inline `mcp:server` token, shows `/server` in the user message, excludes it from skill-path sending, and prefixes the model prompt with a directive to use that connected MCP server when relevant. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Release prep for v0.1.11 (2026-07-02): package version bumped to `0.1.11` for browser loading-bar UX and per-thread browser URL restore across right/bottom docks. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Commit/tag/push in progress.
- Browser per-thread URL follow-up (2026-07-02): browser panel URLs are now stored in localStorage under `devil-codex:browser-session-urls` keyed by `runtime:threadId`. `UtilityPanel` and `BottomDock` pass that key into `ToolContent`, and `BrowserPanel` remounts per key so returning to a thread restores its previous URL while the persisted browser profile keeps cookies/login. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Browser loading UX follow-up (2026-07-02): embedded browser now renders `.browser-load-bar` over the webview whenever `BrowserState.loading` is true, so Enter navigation/link clicks/page moves show a visible progress indicator instead of only changing the toolbar icon. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Release prep for v0.1.10 (2026-07-02): package version bumped to `0.1.10` for the bottom-dock expanded-panel fix and persistent embedded-browser profile toggle. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Commit/tag/push in progress.
- Browser profile persistence follow-up (2026-07-02): embedded browser now defaults to the persistent Electron partition `persist:devil-browser` so cookies/login/localStorage/IndexedDB survive app restarts. Settings -> `구성` adds `브라우저 프로필 저장`; turning it off remounts the webview into a guest partition (`devil-browser-guest`). Existing guest-session logins do not migrate; sign in once in the persisted profile. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Post-v0.1.9 local hotfix (2026-07-02): right utility expanded mode no longer sets `.bottom-dock` to `opacity: 0`, which previously left the bottom dock height as a blank black strip when the bottom dock was open. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Release prep for v0.1.9 (2026-07-02): README now documents background notifications and per-thread/right-bottom panel persistence; stray untracked `test.md`, `test.txt`, and `test2.md` were removed; `package.json` and `package-lock.json` were bumped to `0.1.9`. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Commit/tag/push in progress.
- Background notification follow-up (2026-07-02): Electron main now exposes `app:notify`, showing OS notifications only when the Devil Codex window is hidden/minimized/unfocused and focusing the app when clicked. Renderer emits notifications for final turn completion/failure, approval requests, and ask-user questions, with toggles under Settings → `알림` persisted in `devil-codex:settings`. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Bottom-dock per-thread follow-up (2026-07-02): bottom dock state now mirrors right utility panel behavior by storing bottom `tabs`, active tab, open/closed state, and height per `runtime:threadId`. Thread switches, settings navigation, and Codex/Claude runtime switches snapshot/restore the bottom dock without leaking one thread's bottom tabs into another. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Activity status follow-up (2026-07-02): successful turns with an internal failed command/search no longer display as `작업 실패`. `TurnActivity` now uses the turn item's own status for the top-level failed label instead of any failed child entry, and timeline merge lets a newer turn status replace stale failed state. Individual failed commands still render as failed inside the expanded activity. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Steering follow-up (2026-07-02): forced queued steering now differs from ordinary queued follow-up. When the user presses the queued-row steering button, the pending turn text is prefixed with an explicit instruction to continue the current work and apply the new direction rather than just answer the message. The interrupted prior turn id is tracked, its post-completion history sync/final-answer recovery is skipped, and `startQueuedTurn` restores busy state for the visible thread. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Post-v0.1.7 local scroll fix (2026-07-02): thread opens now avoid visible "scroll down" motion by removing `.thread-view` smooth scrolling and hiding cached thread content with `.thread-view.initializing` until the initial saved/bottom scroll is applied in `useLayoutEffect`. Uncached thread opens keep `loadingThreadId` through navigation and show a top loading bar + loading state until history resolves, then initialize bottom before revealing content. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Loading UX follow-up (2026-07-02): uncached thread opens now track `loadingThreadId` and render a dedicated conversation-loading placeholder instead of briefly showing the empty-thread state before history arrives. Side-chat creation no longer reuses the old launcher/list; it shows a small global "사이드 채팅 준비 중" toast while `createThread` runs, and side-chat history loading uses a skeleton panel. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Post-v0.1.6 local scroll polish (2026-07-02): opening a thread now applies the initial saved/bottom scroll position synchronously inside `useLayoutEffect` before first paint, then repeats on the next animation frame for stabilization. This avoids the visible "render at top then scroll down" effect; new/bottom-pinned threads should appear already at the bottom. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Thread panel-state/runtime continuity follow-up (2026-07-02): right utility panel state now stores `tabs`, active tab, `open`, and `expanded` per `runtime:threadId`, so switching threads restores open tabs only if that thread's panel was actually open; closed threads stay closed even when they still have remembered tabs. Runtime switching saves the current runtime's active thread snapshot and restores the target runtime's last active thread without overwriting panel open/closed state. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Right-panel scroll follow-up (2026-07-02): opening a right utility tab used to resize the chat column and trigger composer/layout bottom-pinning, so the thread scroll moved even when the user expected the current view to stay put. `src/renderer/main.tsx` now captures `scrollTop` before `openUtility`, suppresses automatic bottom correction during the panel transition, restores the same position over several delayed layout passes, then restores the previous bottom-follow state. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Runtime switch/scroll follow-up (2026-07-02): switching Codex/Claude now snapshots the current runtime's active thread/workspace/items and restores that snapshot when returning, so a running Codex thread can continue showing live cached activity after visiting Claude mode. Thread bottom pinning now writes exact `scrollHeight - clientHeight` and stabilizes across several frames plus short delayed passes, preventing new/opened threads from stopping slightly above the true bottom. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Post-v0.1.5 local regression fix (2026-07-02): the side-chat loading workaround had accidentally reopened the old side-chat launcher/list UI. `src/renderer/main.tsx` no longer opens the `side-chat` launcher while creating a side chat, side chat state keys include `runtime:threadId`, right-panel restore keys also include runtime, and switching Codex/Claude runtime closes right and bottom docks. `src/renderer/components/ToolContent.tsx` no longer renders the side-chat/subagent list inside the `side-chat` tool screen. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass locally. Not committed/pushed/versioned yet.
- Local pre-release UI fixes (2026-07-02): user explicitly said not to version bump/upload until asked. Local changes fix renderer issues: `TurnActivity` chevron hit target, thread scroll restoration, composer height clearance, running-thread history hydration, and provider diagnostic preservation. DeepSeek's provider-diagnostic patch was corrected: known active `turnId` is injected before timeline mapping, activity entries merge by id to preserve failed diagnostics, and `recoveredHistory` is locally scoped. Follow-up root `tsconfig.json` typecheck failures were fixed in renderer components/main/settings. Provider failure text rows are now deduped both when timeline entries are inserted and when old persisted activity rows are rendered. User prompt rows are preserved across completion/sync: renderer caches optimistic user items under the real thread id, periodic sync merges instead of replacing, main history-cache preserves cached user/agent/system items, and provider transcript merge keeps local prompts in order. Edit-card action text is smaller. Side-chat/subagent panels now use the active runtime/provider/model/account context, create Claude Code side chats in Claude mode, and read/send with runtime/account instead of falling back to Codex defaults. Side-chat/subagent model picker now mirrors main picker hierarchy enough for side panels: providers contain account rows, account-specific model lists, selected account/model checks, per-account pagination, and selected `accountId` persists in `subagentPick`. New side-chat creation no longer waits silently on `createThread`: the launcher opens immediately, duplicate clicks are blocked, and the button shows a spinner/loading label until the real side-chat tab is ready. Provider usage refresh used to be active-view/auth/focus/request-log driven while main-process quota APIs had 90s caches; now `providerUsage({force})` exists, Antigravity's separate quota cache is cleared too, and opening usage/manual refresh/turn completed/request completed paths bypass cached quota values. `npx tsc -p tsconfig.json --noEmit`, `npm run build`, and `git diff --check` pass under `devil-codex@0.1.4`. Not committed/pushed/versioned yet.
- v0.1.4 composer isolation fix (2026-07-02): root cause was that `Composer` draft text/attachments used a thread key, but model/provider/account/reasoning/speed came from global App state (`providers.settings`, `claudeModel`, global reasoning/speed). A model chosen in one thread therefore appeared in another thread's prompt bar. `src/renderer/main.tsx` now persists composer config by `runtime:thread:<id>`, uses that config before global defaults, keys existing-thread drafts by runtime+thread id, and only shows busy/stop state for the active thread id. `npm run build` passes under `devil-codex@0.1.4`.
- v0.1.3 usage quota fix (2026-07-02): Provider quota surfaces now display remaining percent (`% 남음`) instead of used percent, matching stock Codex's "남은 사용량" rows. `provider-usage.cts` preserves used/remaining internally but parses `remaining_percent` as remaining, supports fractional 0..1 percentages, and recognizes Claude Fable windows from model/limit/tier/bucket hints so real Fable usage is not replaced by the old 0% fallback. `npm run build` passes under `devil-codex@0.1.3`.
- v0.1.3 work timer fix (2026-07-02): `TurnActivity` now treats either the parent turn or any child activity with `status: "inProgress"` as running, and only shows failed once no work is still running. This keeps the "동안 작업 중/실패" duration ticking live instead of jumping at final completion/failure.
- v0.1.2 release prep (2026-07-02): `package.json` and `package-lock.json` bumped from `0.1.1` to `0.1.2` for the tag-triggered GitHub release action. `npm run build` passes under `devil-codex@0.1.2`.
- Stock restart continuation fix follow-up (2026-07-01/02): user confirmed immediate same-thread follow-up works, but after quitting/reopening stock Codex the same thread could not continue until switching away/back. DB/global-state repairs alone were incomplete: stock Codex logs showed resumed turns still running `approval_policy=Never` + `DangerFullAccess` because the session rollout JSONL `turn_context` lines persisted stale `never`/danger/full-access/disabled values and were reused on resume. `src/main/main.cts` now records cwd/permissions in pending diagnostics and calls exported `syncStockThreadPermissions` on terminal turn events. `src/main/app-server.cts` now writes global-state, SQLite sandbox metadata in stock Codex's current `managed` format, and the thread's rollout JSONL `turn_context`; the rollout matcher now parses each JSONL line and checks `event.type` so spacing/serialization changes are not missed. Safety guard: rollout files over 50MiB are skipped with a warning, and rollout sync failures do not fail normal Devil sends. Live repairs: recent Devil threads were synced to DB/state `on-request`/workspace, 18 rollout files were initially patched plus 7 follow-up rollout files, and target `019f1e0e...` now has `turn_context total=5, bad=0`. Very large old rollout files were skipped for safety. `npm run build` and `git diff --check` pass.
- Dark-mode/stock-continuation follow-up (2026-07-01): test1/test2 confirmed live shared Codex defaults are now `model=gpt-5.5`, `model_reasoning_effort=medium`, `service_tier=default`. Current `~/.codex/config.toml` had lost `[desktop] appearanceTheme`, while many backups still had `appearanceTheme="dark"`, so `src/main/codex-desktop-theme.cts` now can recover a missing desktop theme from Codex/Devil config backups and `src/main/codex-config.cts`/`codex-settings.cts` call that before rewriting managed config blocks/settings. Live config was restored to `appearanceTheme="dark"`. Recent stock-continuation suspect threads `019f1dfa...` and `019f1e01...` had DB `approval_mode=never`, `sandbox_policy={"type":"disabled"}` despite shared config being `on-request/workspace-write`; live DB rows were repaired to `on-request` + workspace-write. `src/main/app-server.cts` now writes permission sync into both `.codex-global-state.json` and `state_5.sqlite` after `turn/start`, so future Devil turns should not leave stock Codex with stale disabled/full-access thread metadata. `npm run build` and `git diff --check` pass.
- Codex model/reasoning/speed sync fix (2026-07-01): verified the current stock rollout for thread `019f1dfa-105f-7040-be55-b93ddd4329bd` has two turn contexts: first `effort:xhigh`, then Devil-sent `effort:medium`, so the latest Devil turn was actually sent with medium reasoning. Root cause of the mismatch was UI/default-state sync, not the `turn/start` payload. `CodexSettings` now manages `model_reasoning_effort` and `service_tier` in shared `~/.codex/config.toml`; renderer model/reasoning/speed changes immediately sync Codex defaults. Live config was set to `model=gpt-5.5`, `model_reasoning_effort=medium`, `service_tier=default`. `npm run build` passes.
- macOS sidebar/titlebar follow-up (2026-07-01): when the project sidebar is collapsed on macOS, `src/renderer/main.tsx` now renders `MacCollapsedNav` inside the topbar beside the native traffic lights with sidebar-open/back/forward buttons. This avoids the earlier fixed overlay being swallowed by Electron's `-webkit-app-region: drag` hit testing. `src/renderer/styles.css` adds a `topbar-left` group and no-drag button styling. Windows titlebar behavior remains unchanged. `npm run build` passes.
- Stock-Codex settings contamination root cause + fix (2026-07-02): two interlocking causes. (1) `CodexSettingsStore` "legacy cleanup" stripped `service_tier` on **every load()** — but stock Codex stores its own response-speed choice in that key, so Devil kept resetting stock's speed (보통 → 빠름) and rewrote config.toml constantly (Claude turns call settingsStore.load()). (2) All Devil config.toml writes used truncate-style `writeFile`; the bundled app-server / stock desktop can read mid-write, re-serialize the broken parse, and permanently drop root keys — reproduced from `devil-codex-backups`: backup 03:18:20 had `model`, `model_reasoning_effort`, `appearanceTheme="dark"`, projects trust; the 03:18:40 marketplace-refresh rewrite lost them all (theme→white, effort display change). Fix: `service_tier`/`model_reasoning_effort` are never touched; `writeCodexConfigAtomic` (temp+rename) used by `codex-settings.cts` and all `codex-config.cts` register/unregister paths; writes are skipped when content is unchanged (register is now idempotent). The damaged live `~/.codex/config.toml` was restored from the backup (model gpt-5.5, effort medium, service_tier default, dark theme, trust entries). Fakehome smoke: 3 register/load/unregister cycles preserve all stock keys and skip-unchanged holds.
- Claude Code approval bridge (2026-07-02): Devil no longer relies on permissionMode "auto" self-judgement. `claude-runtime.cts` passes SDK `canUseTool`; Claude Code's own permission engine decides when to ask, and the prompt is emitted as the same `item/commandExecution|fileChange/requestApproval` events the Codex app-server uses, so the existing renderer approval dialog answers it (`approval:respond` routes `claude-approval-*` ids to `ClaudeCodeRuntime.respondApproval`). Modes: full access → bypassPermissions (no bridge), read-only → default, normal → acceptEdits. acceptForSession returns SDK permission suggestions as updatedPermissions; interrupt/turn-end cancels pending prompts. Live E2E: Write-tool prompt fired, decline blocked the file, accept created it.
- Claude Code mode gap/UI pass (2026-07-02, second pass): composer image attachments (data URL or file path) are converted into Anthropic image content blocks in `claude-runtime.cts` (`userMessageContent`/`imageBlock`, unsupported formats skipped safely); the `maxTurns: 100` cap was removed; Claude SDK turns now record into the shared provider request log via new `CodexProxyServer.recordRuntimeRequest`/`finishRuntimeRequest` (route `claude-agent-sdk`, usage from the SDK result), so Settings → 연결 and the environment usage card cover Claude turns. Renderer: composer context gauge now shows in Claude mode (uses `composerModel`; `modelContextWindow` knows sonnet/opus/haiku/fable = 200k), ModelPicker hides Codex-only 추론/속도 controls and the trigger effort label for `runtime === "claude-code"`, and the Claude slash menu adds prompt-based/runtime-agnostic commands (memory, init, review, pet). `npm run build` passes; live haiku E2E verified text-only and image-attachment turns both return usage.
- Claude Code runtime hardening (2026-07-02): `src/main/claude-runtime.cts` probe now detects the Agent SDK's bundled per-platform `claude` binary (`@anthropic-ai/claude-agent-sdk-<platform>-<arch>`), so an externally installed Claude CLI is no longer required; version comes from the SDK's `claudeCodeVersion`. Real bug fixed: tool results arrive inside SDK **user** messages, which were previously unhandled, so command/tool rows never resolved — they now complete/fail with output. Tool calls emit `item/started` (in-progress spinner), thinking deltas stream as `item/reasoning/summaryTextDelta`, consecutive narration text blocks are separated with a blank line, and result usage/context-window is attached to `turn/completed` so token gauges work. Session ids are SDK-auto-generated and saved early via a new `onSessionId` callback (forcing `sessionId: threadId` broke retry after a failed first turn); user stop now resolves quietly with an `interrupted` turn instead of rethrowing into a renderer 요청 실패 row. Replay/subagent tool events are filtered by known tool-run ids and `parent_tool_use_id`. Dead `claude -p` line-parsing code removed. `npm run build` passes; live haiku E2E verified simple + bash-tool turns end-to-end.
- Claude Code runtime SDK update (2026-07-02): `src/main/claude-runtime.cts` now uses `@anthropic-ai/claude-agent-sdk` streaming input instead of direct `claude -p` subprocess parsing. It keeps Devil thread/session metadata, emits partial assistant deltas, maps tool calls/results to existing activity UI, uses `AbortController` for stop, and passes `mcpServers` from Devil's Claude MCP config. `src/main/main.cts` passes `devil_ask` into Claude Code; `devil_browser`/`devil_computer` remain behind the existing Devil MCP toggle. `package.json` adds SDK dependency and asar unpacking for SDK binaries. `npm run build`, SDK dynamic-import smoke, and touched-file `git diff --check` pass.
- Claude mode UX follow-up (2026-07-02): `src/renderer/main.tsx` exposes Claude-only `/browser-use` and `/computer-use` virtual runtime skills in the composer and turns selections into explicit Devil MCP instructions. `src/main/main.cts` now forces the matching `devil_browser`/`devil_computer` MCP server into that Claude SDK turn even if the global Devil MCP toggle is off. Claude slash suggestions hide Codex-specific commands. Renderer saves Claude live activity history but skips post-turn sync replacement for Claude so work tabs are preserved and flicker is reduced. `npm run build` and touched-file `git diff --check` pass.
- Claude Code usage UI follow-up (2026-07-02): `src/main/provider-usage.cts` now recursively parses Anthropic OAuth usage windows and normalizes Claude labels toward CLI-style buckets like current session/current week/Fable when present. `SettingsView` opens Provider limits first and displays used percent; the account menu prefers the selected provider so Claude Code mode shows Claude Code usage before Codex. `npm run build` and touched-file `git diff --check` pass.
- Runtime icon polish (2026-07-02): the runtime switch and account menu now use the provided Codex/Claude PNG icons from `src/renderer/assets`, short Codex/Claude labels, and rounded icon styling. Claude usage fills in a 0% Fable weekly row when the Anthropic usage response omits it. `npm run build` and touched-file `git diff --check` pass.
- Terminal clipboard paste fix (2026-07-01): `src/renderer/components/TerminalSession.tsx` no longer blocks native paste events for Ctrl/Cmd+V; it captures the real `paste` event and reads `event.clipboardData` first, while toolbar/context-menu paste falls back from preload `clipboard.readText()` to `navigator.clipboard.readText()`. `src/renderer/styles.css` moves terminal clipboard errors into a styled toast below the toolbar so they no longer overlap the terminal title/path. `npm run build` and touched-file `git diff --check` pass.
- v0.1.0 release prep (2026-07-01): `package.json` and `package-lock.json` are bumped from `0.0.34` to `0.1.0` after the main feature set was considered ready. `npm run build` passes under `devil-codex@0.1.0`; package/source/docs search found no remaining `0.0.34` references in the checked paths, and touched package-file `git diff --check` passes.
- Activity diagnostic visual polish (2026-07-01): `src/renderer/components/TurnActivity.tsx` now detects diagnostic-like provider failure messages even when they arrive as generic activity messages, and renders them with a muted diagnostic text style. `src/renderer/styles.css` tones down provider diagnostic cards/strips from bright white/blue to graphite gray so model/account-route errors do not look like assistant responses. `npm run build` and touched-file `git diff --check` pass.
- Usage model list/pricing polish (2026-07-01): `src/renderer/main.tsx` environment usage card now shows 4 model rows by default and adds a `더보기` toggle to reveal all models; long provider/account/model labels wrap instead of being hidden behind ellipses. `src/renderer/providerPricing.ts` centralizes estimated per-1M token pricing for renderer usage surfaces, and `SettingsView`/main usage cards now share it. Prices were refreshed from public provider pricing pages for OpenAI, Anthropic, Google Gemini, DeepSeek V4, xAI, Groq, Mistral, Together, Fireworks, Moonshot/Kimi, NVIDIA NIM, plus free/local routes. `npm run build` and touched-file `git diff --check` pass.
- v0.0.21 release prep (2026-06-30): `src/renderer/main.tsx` persists per-thread main timeline scroll positions and restores them after switching threads/settings; `Composer` stores per-thread draft text/goal/skills/attachments in localStorage; `SideChat` stores draft text by subagent/side-chat thread id. Renderer native `window.prompt`/`window.confirm` calls were replaced by internal dialogs, and shared CSS now keeps modals inside the Devil window while unifying settings/prompt/modal scrollbars. `package.json`/`package-lock.json` bumped to `0.0.21`; `npm run build` and touched-file `git diff --check` pass. Committed as `d7fbe96`.
- v0.0.20 release prep (2026-06-30): `package.json` and `package-lock.json` are bumped to `0.0.20` for a tag-triggered updater release that includes the speed-setting contamination fix and the multi-bug UI/runtime fix. `npm run build` passes on `0.0.20`.
- Context-window diagnostic fix (2026-06-30): session `019f1147-34ef-7552-9fbe-4f5cf42a7a1c` was already full (`258400/258400`) after several compactions, so compact/retry could not start. `src/main/main.cts` now recognizes token-count events with `total >= model_context_window` and `last_total = 0`, then surfaces a clear "Codex context window is full" Provider diagnostic instead of the generic login/model/quota/network message. `npm run build` passes.
- Multi-bug UI/runtime fix (2026-06-30): context meter root cause was that Devil only attached rollout `token_count` as total/cumulative usage and fell back to estimating all visible timeline text after compaction; `src/main/codex-token-usage.cts` and `src/renderer/threadTimeline.ts` now derive `contextUsage` from `last_token_usage + model_context_window`. Steering queued messages were only shown in the composer queue panel until `startQueuedTurn`; `src/renderer/main.tsx` now renders queued turns as pending user bubbles without inserting them into the committed timeline early. Settings reopened the right panel because `restoreNavigation` restored per-thread utility state after `openView("settings")`; Settings now suppresses utility restore. Tray root cause on installed Windows app was missing `resources/build/icon.ico/png`; `package.json` now extraResources those files and `src/main/main.cts` has non-empty tray fallback. Clipboard image replacement was caused by all pasted files sharing `image.png` path; `Composer` now gives clipboard files unique virtual paths while preserving real-file dedupe. `npm run build`, context snapshot smoke, icon resource smoke, and touched-file `git diff --check` pass. Not committed yet.
- Superseded speed setting attempt (2026-06-30): an earlier fix tried to keep response speed only in Devil localStorage and strip `service_tier` from shared config. This was superseded on 2026-07-01 because stock Codex needs the shared `service_tier`/`model_reasoning_effort` defaults to match Devil's visible picker state.
- Update detection fix (2026-06-30): GitHub latest release API returned published `v0.0.19` with Windows installer and `latest.yml`, so the feed was valid. `src/renderer/main.tsx` now calls `window.devilCodex.checkForUpdates()` immediately after subscribing to `onUpdateState`, preventing missed startup events. `src/main/auto-update.cts` now polls every 5 minutes while packaged instead of every 6 hours, so a background-running old app can notice a freshly published release without restart. No version bump; `npm run build` passes.
- Windows tray/release fix (2026-06-30): v0.0.19 adds packaged `build/icon.ico`, resolves packaged/app resource icon paths before creating tray/window icons, uses `nativeImage` resized to 16px for the Windows tray, and routes tray/menu/window Exit through `quitApp()` which destroys the tray and calls `app.quit()`. `src/main/contracts.cts` now matches shared `WindowControlAction` with `quit`. `npm run build` and `git diff --check` pass; tag `v0.0.19` should trigger the installer workflow after push.
- Environment thread-token fix (2026-06-30): `src/main/codex-token-usage.cts` reads Codex rollout `token_count.total_token_usage`/`last_token_usage` snapshots by thread id and attaches them to `ThreadHistoryItem` as `cumulativeTokenUsage`/`tokenUsage` during `thread:read` and `thread:sync-history`. `src/renderer/main.tsx` no longer uses `contextUsage.usedTokens` as the current-thread total; the environment card now shows total thread usage from real usage fields and displays current context as a separate secondary gauge. `npm run build` passes, and a smoke check for thread `019f0efd-0c74-7551-94e6-2ce3dc6db4c7` read cumulative `810598` tokens.
- IPC readiness race fix (2026-06-30): installed v0.0.17 contained the handler and normal startup `createWindow()` was after handler registration, so `No handler registered for 'runtime:status'` could only come from early `showMainWindow()` paths (`second-instance`/tray/activate) creating a renderer before IPC registration completed. `src/main/main.cts` now gates `showMainWindow()` behind `ipcHandlersReady`, queues early show requests, creates the tray only after handler registration, and then creates/shows the window. Version bumped to `0.0.18`; `npm run build` passes, built order check shows `runtime:status` before startup `createWindow()`, and local `npm start` spawned renderer plus `vendor/codex/codex.exe app-server --stdio`.
- Startup unblock hotfix follow-up (2026-06-30): installed v0.0.16 had the correct app.asar and bundled Codex 0.142.4, and direct installed `codex.exe app-server --stdio` initialized in ~267ms. Fresh app launch still produced no renderer/app-server child because `app.whenReady` awaited `startCodexProxy()` before `createWindow()`. `src/main/main.cts` now starts provider/MCP registration in the background, registers IPC handlers before loading the window, then calls `createWindow()`/`initAutoUpdate()`. `src/renderer/main.tsx` catches runtime/connect IPC failures and surfaces an error instead of leaving `Codex app-server 시작 중` stuck. Version bumped to `0.0.17`; `npm run build` passes and local `npm start` spawned renderer plus `vendor/codex/codex.exe app-server --stdio`.
- App-server startup hotfix (2026-06-30): v0.0.15 could stay on `Codex app-server 시작 중` because bundled Codex 0.142.3 did not answer the stdio initialize request in local handshake tests. `0.142.4` answered in ~260ms, so `.github/workflows/release.yml` now bundles `rust-v0.142.4`, `vendor/codex/codex.exe` is updated locally, and `src/main/app-server.cts` starts with explicit `--stdio`, disables remote control for embedded Devil sessions, sends the current initialize capability shape, and times out/cleans up stuck initialize requests after 30s. Version bumped to `0.0.16`. `npm run build` passes; direct wrapper connect reached `connected` in ~252ms.
- Usage surfaces polish (2026-06-29): `src/renderer/main.tsx` now reuses provider usage/report-log data in the account menu and environment popover. Account `남은 사용량` expands inline with compact quota windows, while the environment popover shows current-thread token totals, context max when available, and per-model request-log rows. `src/renderer/styles.css` adds compact inline usage and thread-token presentation. `npm run build` passes.
- Background/run continuity fix (2026-06-29): `src/renderer/main.tsx` now writes active thread items into `threadHistoryCache` before navigation snapshots and restores from the latest cache, preventing Settings/back from replacing streamed work with stale items. `src/main/main.cts` now creates a tray icon, treats window close as hide-to-background, disables renderer background throttling, restores hidden windows from tray/second instance/activate, and only disposes Codex servers on explicit `quit`. `src/shared/contracts.ts` adds the `quit` window action; Windows `Exit` now uses it. `npm run build` passes.
- Edited-message restart UX (2026-06-29): because Codex app-server has no message edit/truncate API, Devil keeps the safe new-backend-thread approach but now treats it like an in-place restart in the UI. `src/renderer/main.tsx` hides the replaced thread locally, carries pin/side-chat/right-panel state to the new thread, filters hidden replaced threads from search, and blocks historical edits while a turn is running. `npm run build` passes.
- Edited-message thread title fix (2026-06-29): continuation prompts still include prior context for the model, but `src/renderer/main.tsx` now renames edit-created threads from the edited user prompt. `src/main/app-server.cts` strips `[수정된 사용자 메시지]` context from native list/search/project titles and previews, `src/main/main.cts` does the same for external-provider transcript title/preview generation, and `src/main/codex-session-index.cts` prevents `session_index.jsonl` names from reintroducing the internal context. Long titles are compacted with `...`. `npm run build` passes.
- Main chat auto-scroll fix (2026-06-29): `src/renderer/main.tsx` now reruns the thread bottom-pin layout effect when `items` changes, not only when `items.length` changes. This covers streaming AI output and activity text deltas that update an existing timeline entry without appending a new message. The existing `stickToThreadBottom` guard still preserves manual upward scrolling. `npm run build` passes.
- Activity body live-status placement (2026-06-29): `src/renderer/components/TurnActivity.tsx` now renders the running `생각중/작업 중` banner after the activity summary and activity entries, so the expanded work body keeps the live status near the bottom instead of at the top. `src/renderer/styles.css` adds a tiny bottom-banner spacing class. `npm run build` passes.
- Theme persistence fix (2026-06-28): stock Codex could reopen in light mode because Devil managed `~/.codex/config.toml` by deleting whole BEGIN/END marker ranges for provider/MCP blocks. Over time, stock Codex had inserted `[mcp_servers.node_repl]`, `[desktop] appearanceTheme`, plugin, and feature sections between Devil's ask-MCP markers, so Devil quit/startup could erase the desktop theme section and force stock Codex back to default white. `src/main/codex-config.cts` now removes only Devil-owned tables/comments (`model_providers.devil`, `mcp_servers.devil_*`) instead of whole marker spans, and the live `config.toml` ask block was normalized so `[desktop]` is outside the managed region.
- Permission flow fix (2026-06-28): full-access warning dialog is portaled to `document.body` so it no longer clips inside the composer/chat pane; composer approval mode persists via `localStorage`; existing-thread turns forward approval/sandbox values to `turn/start` with a compatibility retry; approval responses are routed by request id back to the per-thread app-server that emitted the approval request so accepting a modal should let the AI continue.
- Electron + React/TypeScript product shell and Codex app-server bridge exist.
- Product direction and architectural constraints are agreed: Codex parity is the primary acceptance criterion, with multi-provider support as the extension.
- M4-A packaging/update and M4-B subagents/side-chat are complete; M4-C direction was corrected after user clarification. The short-lived Devil-local Automations MVP (`AutomationStore`, IPC, app-local scheduler, create/list/toggle/delete/run UI) was removed because the target is **stock Codex-backed Automations**: create/manage from Devil, but execution/sync must go through the same stock Codex/ChatGPT automation path. Current Automations UI now follows stock Codex's first-run behavior: "채팅으로 만들기" and template chips create a new Codex-direct project chat and send a prepared automation setup prompt even if the current composer model is external; there is still no Devil-local scheduler/storage. Probe confirmed Codex app-server v0.142.0 does not expose `tasks/*`, `automation/*`, or `app/automations` methods; `~/.codex.state_5.sqlite` `agent_jobs` appears to be CSV/agent-job tooling, not the user-facing Automations feature. First release test `v0.1.1` uploaded Windows update assets but the workflow ended red because macOS DMG/ZIP publishing raced on the same GitHub Release; `v0.1.2` fixes this by making macOS builds artifact-only while Windows remains the release/update-feed publisher. `v0.1.3` is the Windows polish/update test target: Settings reads the real packaged version, non-macOS builds render traffic-light-style close/minimize/maximize controls, and project rows display cross-platform folder basenames instead of full Windows paths. Windows artifact/update verification remains user-side after CI publishes the release.
- M3 providers (in progress, commit `d459a9d`): connection settings split into two sections — login providers (Codex, Claude Code, GitHub Copilot) and API-key providers (OpenAI, Anthropic, Google, DeepSeek). Connection tab manages credentials only (local viewing state) and no longer changes the composer model picker; the model section was removed.
- In-app OAuth (rcodex-style, no external CLI) for the two login providers: Copilot uses the GitHub device flow → Copilot token; Claude Code uses Anthropic PKCE OAuth + localhost:49155 callback → creates an API key (falls back to the OAuth access token as Bearer with `anthropic-beta: oauth-2025-04-20` + Claude Code system prompt). Tokens are encrypted via safeStorage in `provider-oauth.cts`. Codex stays on the app-server (its login is reused from an installed `codex`).
- Chat routing: codex turns go through the app-server; external-provider turns go through `provider-runtime.cts` (`dispatch`): claude-code → `claudeChat`, copilot → `copilotChat`, API providers → keychain key. Dynamic model lists: codex via app-server `model/list`, API providers via each `/models` endpoint (auto-synced when a key exists), login providers via OAuth `/models`; `useProviders` merges these over the static catalog.
- Top "새 채팅" and app startup/connect both enter a standalone (general) chat under `~/Documents/Codex/<date>/new-chat` so the first composer is Codex general chat, not an active project draft.
- Windows UI polish is in progress: non-macOS now has a stock-Codex-like title/menu bar, side-chat uses the main model-picker style, activity rows animate/expand reliably, Provider diagnostics are compact graphite cards, and `다음으로 열기` dynamically lists installed/openable apps. Fresh empty `new-chat` screens hide the thread ellipsis and disable `다음으로 열기` so the app no longer tries to open the pseudo `.../new-chat` folder. Sidebar thread rows now separate hover menu/time spacing, show a small spinner for running threads, and route offscreen turn events into per-thread history cache so multiple active conversations do not visually mix. Thread rename follows stock Codex semantics: it changes `~/.codex/state_5.sqlite` `threads.title`, not the real workspace folder name.
- Plugins/Skills page now behaves as a read-only stock-Codex status surface: the Plugins tab lists connected MCP/plugin servers from the shared Codex app-server state without add/manage/run actions, while the Skills tab lists enabled `skills/list` results with search and refresh.
- Search now uses a Codex-like command palette overlay instead of navigating to a separate page. It merges recent general/project chats by recency, remote `searchThreads` results while typing, Cmd+1..9 quick-open shortcuts, and only connected commands (new chat, folder open, settings, file search, active-thread archive/pin/side-chat, plugins/MCP, review, find/back/forward).
- External-provider chats (Claude Code/Copilot/API) are stored in devil-codex's own `provider-transcript.cts` store (thread summary + transcript) and merged into the sidebar. Recovery/startup races were fixed (`b3177de`, `b497e8d`), so Devil-local reopen is the supported path.
- External login-provider threads now use the app-server `modelProvider: "devil"` Responses proxy only; the forbidden `codex-mirror.cts` SQLite/session injection layer was removed (`20a2fc2`). Copilot rejects more than 128 tools, so both Copilot and Anthropic adapters now limit and normalize tool schemas (`f162512`).
- Electron verification: Copilot app-server turn completed through the proxy with 58 Codex tools; a direct 191-tool Copilot request also completed after limiting. Codex `gpt-5.4` completed without any proxy traffic. Claude Code OAuth currently returns Anthropic 401 and needs re-login. Devil-local provider threads reappeared after restart.
- Accepted sync direction changed on 2026-06-24: implement a narrow rcodex-style provider reconcile layer. Codex turns remain vanilla/no proxy. External turns run through app-server `modelProvider: "devil"` and Devil proxy, then the existing thread's provider metadata is reconciled back to `openai` in `~/.codex/state_5.sqlite` + rollout `session_meta` so stock Codex can list the same thread. This requires journal-first pending tracking, schema guard, backup-before-write, retry/backoff, startup recovery, and Devil-local actual provider/model metadata. Detailed plan: `.memoc/wiki/knowledge/topics/external-provider-sync-plan.md`.
- First implementation landed locally: `src/main/codex-provider-reconcile.cts` adds pending journal, schema guard, backup-before-write, SQLite/rollout provider patch, retry/backoff, and startup pending recovery; `app-server.cts` can now resume a thread with `modelProvider`; external turns in `main.cts` mark pending, resume as `devil`, run through proxy, then reconcile on `turn/completed`; `provider-transcript.cts` records actual provider/model sync status. `npm run build` passes. Real Copilot E2E passed on 2026-06-24 while stock Codex was also running: Devil `copilot/gpt-5-mini` returned `OK`, pending journal cleared, SQLite `threads.model_provider` and rollout `session_meta.model_provider` were both `openai`, and Devil metadata marked the turn `synced`. Stock Codex UI inspection is blocked by computer-use safety policy, so user visual confirmation is still needed for sidebar visibility.
- Existing stock-Codex-origin thread continuation is verified after the app-server reload fix: before external turns, Devil patches that thread provider to `devil`, disposes the cached app-server, resumes with `modelProvider:"devil"`, sends the Copilot/Claude proxy turn, then reconciles back to `openai`. Test thread `019ef81f-1335-7562-9833-ebc075728354` with prompt `CONTINUE_WITH_COPILOT_TEST_3. OK only.` returned `OK`; `pending-reconcile.json` was empty; DB and rollout first-line provider were `openai`; Devil provider turn was `syncStatus:"synced"`.
- Follow-up stock-Codex reply error was fixed: after external turns, reconcile now restores DB `threads.model` to the prior non-external Codex model, or the root `~/.codex/config.toml` model if the thread had already been left on an external model. The broken test thread was manually backed up and restored from `openai|copilot:gpt-5-mini` to `openai|gpt-5.5`; a new Copilot turn `MODEL_RESTORE_AFTER_COPILOT_TEST. OK only.` returned `OK`, cleared pending, and ended with DB `openai|gpt-5.5`.
- Devil-local external thread titles now match Codex semantics: they are based on the first user message and are not overwritten by later external-provider turns. For existing native threads that first become external in Devil, the title is derived from the native thread history's first user message when available. Existing bad local provider transcript titles were normalized once.
- Devil sidebar recency now matches Codex's seconds-based timestamp contract: `ProviderTranscriptStore` normalizes `updatedAt` to Unix seconds on load/save/recovery, returns summaries newest-first, and `main.cts` sorts merged native/external `thread:list` and `thread:projects` results by latest activity. Existing local provider timestamps were normalized once.
- Copilot-hosted Gemini empty-turn diagnosis: `gemini-3.1-pro-preview` produced user-message + token-count rollout entries but no assistant item, and Devil provider metadata recorded the turn as `failed`. `bridge.cts` now treats upstream completion with zero assistant/tool output as `response.failed`; `TurnActivity` visibly labels failed activity cards instead of making the turn look like a silent success.
- External-provider failure messaging was improved: Copilot/Claude adapters preserve upstream HTTP status and translate likely causes (login expired, permission/subscription/model unsupported, rate limit, server issue), stream `error` frames are surfaced, and empty assistant-output failures explain likely model/account/context incompatibility instead of a silent failure.
- Claude Code OAuth routing now matches the subscription-oriented route used by opencodex/rcodex more closely: Devil prefers the OAuth Bearer access token over generated API keys, sends `anthropic-beta: claude-code-20250219,oauth-2025-04-20`, and refreshes Claude Code models with the OAuth token instead of falling back to static catalog entries.
- Claude Code provider handling now also imports an existing macOS Claude Code keychain token when Devil has no fresh token, refreshes OAuth access tokens via the stored refresh token, uses the Claude Agent SDK identity prompt, and applies opencodex-style `proxy_` tool-name prefixing for OAuth calls.
- Claude Code OAuth status now avoids a false positive during login: starting the browser flow no longer clears the logout opt-out or auto-imports stock Claude Code credentials until the callback succeeds and Devil stores a token. Status also requires a usable OAuth refresh token when relying on access-token auth.
- Provider credential/model management now ports the safe parts of opencodex/rcodex beyond Claude: Copilot device/token/model calls preserve upstream error bodies and use the same Copilot headers; API-key providers support env fallback for `ANTHROPIC_API_KEY`, `GOOGLE_API_KEY`/`GEMINI_API_KEY`, and `DEEPSEEK_API_KEY`; API-key model refresh uses a short memory cache and falls back to saved/static models for transient `/models` failures while still surfacing 401/403 credential problems.
- Provider/API surface was expanded from the safe opencodex registry subset and cross-checked with rcodex: xAI, OpenRouter, Groq, Mistral, Cerebras, Together, Fireworks, Moonshot/Kimi, Hugging Face, NVIDIA NIM, Ollama, vLLM, and LM Studio now share a common API provider config for model refresh and proxy requests. Local providers are keyless, visible in the picker/settings without credentials, and image sidecar routing respects provider native-image capability. OAuth-only/special providers such as xAI/Kimi OAuth, Azure, custom provider config, and broader rcodex free-tier catalog remain future work.
- Provider availability was tightened after false-positive checks: picker/composer/proxy `/models` now require a verified live model list for API-key, local, Claude Code, and Copilot providers. Key changes and key deletion clear saved model caches, API-key env fallback is active for known env names, startup refresh revalidates configured providers, and 401/403 clears stale models so unusable providers disappear from the picker.
- `.memoc/wiki/knowledge/topics/opencodex-port-plan.md` now includes an explicit OpenCodex-vs-Devil missing-provider table. Current decision remains: avoid full OpenCodex registry/API adoption for now; prefer custom OpenAI-compatible provider support first if provider breadth becomes a priority.
- Settings → 연결 now keeps credential actions local to the selected card: clicking a login/API-key provider expands login, logout, save, delete, or replace controls directly inside that provider tile instead of pushing the controls to a separate lower section.
- Composer model picker now keeps long connected provider/API-key model lists compact by rendering each provider as a collapsible accordion header; expanded providers show 10 models first and reveal 10 more per `더보기` click, while closing the picker resets both reveal/provider-expanded state and then reopens with only the active provider visible. Picker-specific CSS avoids collisions with the Settings provider model menu.
- Sidebar project creation parity pass: the Projects header is always visible with Codex-like section controls, including a left-of-ellipsis all-open/all-closed toggle and a notebook-plus add button; "프로젝트 만들기" modal offers Local, "기존 폴더 사용" opens the native folder picker, and "다음" creates a real local folder under `~/Documents/Codex/Projects/새 프로젝트...` then opens it as a project draft. Project rows use custom notebook-style closed/open icons. Thread creation still goes through the same `cwd`, so Codex/Devil sync remains cwd-based.
- Empty projects are remembered in `devil-codex:local-project-cwds` immediately after choosing/creating a project, so they appear in the sidebar before the first thread exists; re-adding a hidden project removes it from the hidden list. General chats render outside the Projects scroll section under a separate `채팅` heading with spacing.
- Settings Usage & Billing now shows provider usage for logged-in accounts only. Codex reads the existing `~/.codex/auth.json` OAuth access token and calls `https://chatgpt.com/backend-api/wham/usage`; Claude Code uses the stored OAuth access token and calls `https://api.anthropic.com/api/oauth/usage`; GitHub Copilot displays a logged-in unavailable card because opencodex/rcodex did not expose a reliable Copilot quota API.
- Composer paste handling now forces rich clipboard content through plain-text insertion. Copying a rendered chat bubble back into the contenteditable composer should paste only clean text, not nested bubble-like HTML. `npm run build` passes.
- A-plan first pass is implemented: provider models now carry lightweight capability metadata (`tools`, `images`, `webSearch`, `diagnostics`, notes), the model picker shows capability badges/tooltips, turn activity can render `Provider 진단` cards, and Codex-direct/external turns emit diagnostics with route/reconcile/sidecar status. Web-search/vision sidecars are documented as future setting-gated work, not enabled by default.
- A-plan diagnostics visibility pass is implemented: Provider 연결 탭 now shows per-model capability metadata, Settings → 구성 has default-off sidecar toggles/limits, and provider diagnostics are attached to the same `turn/completed` turn when possible so sync refresh is less likely to hide them. Sidecar diagnostics currently report enabled/disabled plus `0/<limit>` request counts; actual web-search/vision sidecar execution remains future work. `npm run build` passes.
- Web-search sidecar now follows the opencodex-style external-provider tool loop rather than the earlier pre-search MVP. When Settings → 구성 → web-search sidecar is ON, Devil injects a synthetic `web_search` function tool only for external providers, intercepts that call, runs native Codex/ChatGPT web_search via forwarded auth, injects the result as a tool result, and re-asks the external model until it produces a final answer or hits the request limit. Provider diagnostics report toolCalls/requests/loops/failures, and each sidecar search is emitted as a visible timeline activity row (`웹 검색: ...`) with expandable query/source/failure detail. Codex direct route remains unchanged. `npm run build` and local scan smoke checks pass.
- External-provider activity parity improved: Gemini API-key tools use a Gemini-specific schema sanitizer that removes `additionalProperties` and other unsupported schema keywords, fixing the `Unknown name "additionalProperties"` rejection. Devil also records workspace Git snapshots before app-server turns and emits a synthetic `fileChange` timeline item after completion when app-server reports only command execution for an external-provider file edit. This makes external turns show the compact Codex-like `파일 N개 수정` / `편집함 path +N -M` activity when files changed.
- Codex and external-provider turns now share the same Codex-like activity presentation in `TurnActivity`: search commands are grouped as `코드 검색 N개`, file/skill reads as `파일 N개 읽음`, shell commands as `... 실행 완료` with expandable output, file changes as compact `파일 N개 수정`, web searches as `웹 검색`, and provider diagnostics as a separate card.
- Activity/provider parity follow-up is implemented: `TurnActivity` now renders `Provider 진단` after all normal activity rows, and the redundant inner `작업 내용` toggle was removed so the elapsed-time row controls expansion. Gemini API-key tool schemas now also remove invalid `required` keys whose names are not present in `properties`, covering the `required[n]: property is not defined` Google rejection. Settings → 연결 includes an MVP recent Provider request log for external proxy turns, and a vision sidecar can turn image parts into Codex-generated text descriptions for text-only external providers when enabled. `npm run build` plus a local Gemini schema smoke test pass.
- Copilot model discovery should stay metadata/cache based, not generation-probe based. A short-lived probe implementation was rejected after checking opencodex/rcodex because it consumes tokens and adds latency. Current direction: keep `/models` filtering cheap (`model_picker_enabled` + `/chat/completions`) and solve Gemini-like empty turns through adapter parsing, error surfacing, or explicit deny/metadata rules only when confirmed.
- M1 end-to-end local thread flow has been verified with an OpenAI turn.
- Existing workspace thread list and thread resume are verified against Codex app-server.
- Diff panel reads actual workspace Git status and now provides selected-file unified line diff through the sandboxed IPC bridge; verified in Electron against seven modified text files.
- Codex screenshot-driven shell now matches the sidebar/main/floating-environment/composer structure; search, settings navigation, `Cmd+G`, and `Cmd+J` were manually verified.
- Screenshot-driven project collapse/menu, editor-open dropdown, account menu, utility split panel, and real Git review summary were manually verified.
- Right and bottom docks now share review/terminal/browser/files/side-chat launcher/content. Both can remain open while flex transitions resize the thread/composer area.
- Terminal now owns a real workspace shell session, accepts keyboard/paste/control input, streams output, supports drag height adjustment, and normalizes CR to LF in pipe-shell fallback mode. Electron UI input created a real `/tmp` file during verification.
- Terminal now uses a Wave-style command block: native text input sends complete commands to the existing PTY, output keeps a scrollback, `Ctrl+C` interrupts, `clear` resets visible scrollback, and a `ResizeObserver` resizes the PTY. This avoids Electron xterm canvas/IME failures. Electron verified visible Korean `echo 한글` input and output.
- Composer suggestions are anchored to textarea caret for both `$` and `/`; slash can select skills or review/status/goal commands. Approval picker matches ask/agent/full modes and full access requires a warning confirmation.
- Project row hover exposes menu/new-chat actions. Project menu mirrors pin/Finder/worktree/rename/archive/remove; project new-chat shows project heading/context. New threads start lazily on first message and pending rows survive eventual-consistency list refreshes.
- Motion/Lucide now power Codex-like transitions and SVG icons; left/right/bottom panels resize interactively and widths persist.
- Renderer domains now split into `Composer`, `BottomDock`, `DockTabStrip`, `TerminalSession`, `ToolContent`, `ToolLauncherMenu`, `UtilityPanel`, and `ThreadList`; `main.tsx` remains composition/state boundary. Composer has approval policy, goal, attachment path, slash, and skill-picker interactions.
- Project menu can now query `thread/list({ archived: true })` and render a dedicated archived-thread view; restore is intentionally not guessed because the app-server method is unverified.
- Settings configuration now reads and explicitly saves `model`, `approval_policy`, and `sandbox_mode` in `~/.codex/config.toml` through isolated main-process store and renderer hook while preserving unknown keys/comments.
- Shared outside-click/Escape hooks now dismiss shell and composer popovers; composer model control matches Codex's reasoning/model/speed nested menu and updates the selected model.
- Settings now uses a dedicated full-window Codex layout with General/Profile/Appearance/Configuration/Personalization/Shortcuts/Usage pages and local persistence.
- UI Codex-parity pass (2026-06-22): palette neutralized (no green tint; bg #181818, sidebar #262626, accents kept); top strip aligned via `main.cts` titleBarStyle:hidden + trafficLightPosition{19,19} + 52px window-nav/topbar; collapsed sidebar shows nav cluster in topbar; layout converted to flex (main-stage = topbar + stage-row(content-col + utility-panel) + terminal); utility/terminal always-rendered and animate open/close via `flex-basis` CSS transition (.24s) so content pushes smoothly, with a `resizing` state class disabling the transition during drag; ThreadMenu rebuilt (icon+label+kbd, dividers); responsive breakpoints 1120/900/720. Pitfall recorded: do NOT animate grid tracks via `@property` in this Electron build — it broke the grid. See worklog `20260621T1610`.
- Codex turn parity now restores historical/live activity, reasoning, commands, file changes, context compaction, approvals, stop control, Markdown/image messages, file tree/code preview, and review cards.
- Review `실행 취소` keeps conversation history and reverses only the selected AI turn's file patches. New/untracked-file undo was manually verified; tracked modify/delete and multi-file conflict protection compile but still require manual verification.
- M2 Git workflow now connected end-to-end (commits `ef7009a..c096d3a`): selected-file stage/unstage and hunk stage/revert, commit + push, local/remote branch list/create/switch, Draft PR via `gh pr create --fill`, inline line comments routed to a Codex review turn, plus worktree list/create/switch, thread search + archive restore, thread rename/fork/pin, and Skills + MCP server status/tool-call integration. PLANS.md "다음 행동" advanced to M3 (provider adapter contract + OS keychain) after one manual Electron regression of the git hunk/worktree/MCP/PR paths. `npm run build` passes; 13 commits unpushed (origin behind).

## Project Snapshot

<!-- memoc:snapshot:start -->
- Last synced: 2026-07-04T08:44:44
- Detected stack: Node.js, React, Electron, TypeScript

### Config Files

- `package.json`
- `tsconfig.json`
- `vite.config.ts`

### Source Directories

- `.claude`
- `.github`
- `assets`
- `dist-electron`
- `docs`
- `scripts`
- `src`
- `vendor`

### Package Scripts

- `dev`: `concurrently -k "npm:dev:renderer" "npm:dev:electron"`
- `dev:renderer`: `vite --host 127.0.0.1 --port 5173 --strictPort`
- `dev:electron`: `npm run build:main && wait-on tcp:5173 && cross-env VITE_DEV_SERVER_URL=http://127.0.0.1:5173 electron .`
- `build`: `npm run build:renderer && npm run build:main`
- `build:renderer`: `vite build`
- `build:main`: `tsc -p tsconfig.electron.json`
- `start`: `electron .`
- `icons`: `node scripts/build-icons.cjs`
- `prepackage`: `npm run icons && npm run build`
- `package`: `electron-builder`
- `dist`: `npm run prepackage && electron-builder`
- `dist:mac`: `npm run prepackage && electron-builder --mac`
- `dist:win`: `npm run prepackage && electron-builder --win`
- `postinstall`: `node scripts/fix-pty.cjs`
<!-- memoc:snapshot:end -->

## Open Tasks

- Use `docs/CODEX_PARITY.md` as the authoritative implementation gap list and update it as features are verified.
- Replace temporary glyph icons and connect settings/search/header/composer controls to app-server behavior.
- Connect terminal/browser/files/side-chat panel placeholders and account menu actions to real backends.
- Connect locally persisted settings to Codex config.toml and real account/profile/usage data.
- Implement thread search and archive state from app-server data.
- Replace browser/files/side-chat placeholders with real backends and extend Git review with split diff, inline comment, and stage/revert actions.
- Connect persistent worktree creation and project removal/rename to full backend semantics; current rename/pin are local UI state and worktree is explicit placeholder feedback.
- Map app-server items, approvals, and file changes to Codex-parity UI components.
- Define provider adapter contract and API-key providers after OpenAI flow is stable.
- Define an upstream sync process for regularly incorporating `openai/codex` updates.
- Manually verify tracked-file modify/delete undo, multi-file atomic undo, and user-edit conflict rejection.

## Completed Tasks

See `.memoc/worklog/` for full shared activity history.

## Commands

- Memory summary: `.memoc/bin/memoc summary`
- Memory refresh: `.memoc/bin/memoc update`

## Notes

- `npm run build` passes for Electron main/preload and React renderer.
- Composer image attachment parity is in progress: pasted/dropped/selected images render as prompt-top thumbnails and are sent as app-server `type:image` items with `url` data URLs; manual runtime verification with Codex direct and external provider/vision sidecar is still needed.
- User-message attachment parity is now in progress too: display metadata lets Devil render sent image/file attachments as Codex-like cards instead of `- image.png` text, images can open a large copy/save viewer, and long pasted text is converted into a text-file attachment card while still being included in the model prompt. Manual UI verification is still needed.
- OpenCodex attachment handling was compared on 2026-06-25. Useful part: preserve `input_image` as a structured image part through provider adapters, never inline base64 into text. Devil now carries image `detail`, preserves `input_image.file_id` as a marker, decodes text-like `input_file.file_data` into context when present, forwards image `detail` to OpenAI-compatible/Copilot payloads, and lets Anthropic use remote image URLs as native URL image sources. OpenCodex itself mostly treats non-image files as markers, so PDF/document content extraction remains a Devil-specific future layer.
- Attachment metadata restore and Provider dashboard/log pass is implemented. External-provider user turns persist display attachment metadata into Devil transcripts, `thread:sync-history` merges newer native Codex history while preserving local user attachments, and compacted-turn retry keeps attachment metadata. Provider request logs persist under Devil userData, include thread/model/route/tools/images/files/capability/errorType/sidecar stats, and Settings → 연결 computes per-model runtime compatibility from recent request outcomes.
- Document attachment extraction is now the active priority over dashboard/model-compatibility polish. `src/main/document-attachments.cts` enriches file attachments at `turn:send` time so Codex direct and external-provider routes receive extracted document context. First pass supports text-like files, `.rtf`, `.docx`, and best-effort `.pdf`; scanned/complex PDFs and rich binary formats remain future work.
- Timeline change cards are now turn-scoped: agent message cards use that turn's `fileChange` activity instead of the whole uncommitted workspace diff.
- External provider failed-turn diagnostics now distinguish "provider final response failed" from already-executed command/file actions, reducing confusion for Gemini quota failures after successful shell/file commands.
- Post-v0.1.16 local fixes now include Claude Code JSONL session lookup by filename fallback for sanitized/non-ASCII project folders, Claude import filtering for caveman hook banners, forced `sdk-cli`→`cli` normalization during Claude import, visible chat scrollbar strengthening, and shared project alias rename from header/sidebar menus.
- New project creation now asks for a project name before creating the folder and saves it as alias. Renderer persists running thread ids to localStorage so Ctrl+R can restore in-progress UI state while main process continues the turn.
- Bottom dock resize now triggers thread bottom stabilization when the user is at the bottom, preventing the composer from covering the newest chat content.
- Steering interrupt ordering now suppresses late item events from the interrupted turn and removes its stale standalone agent answer before sending the queued steering prompt.
- Composer `/` suggestions now include Codex plugin skills plus Claude MCP/plugin sources, and Claude JSONL import keeps tool-adjacent assistant narration inside the activity timeline instead of duplicating it as final chat.
- Provider quota cards now display used percent like Claude CLI, and Claude usage parsing recognizes more usage-percent field names plus nested parent context for Fable buckets while avoiding fake 0% Fable rows.
- Timeline merge dedupes final agent answers semantically by turn/text, preventing live item + synced history from rendering the same final answer twice.
- Open terminal tabs now expose a shell picker in the terminal toolbar; selecting a profile reconnects that tab with Auto/WSL Bash/Git Bash/PowerShell/cmd instead of requiring Settings-only default shell changes.
- Windows chat thread scrollbar is forced visible with dedicated `.app-shell.is-windows .thread-view` rules; `.thread-view` is no longer part of the shared thin-scrollbar group.
- Devil subagent MCP now exposes `delegate_subagent` through a managed `devil_subagent` config block, stdio script, Electron control pipe, Settings toggle, and hidden provider/runtime delegate path.
- `delegate_subagent` hidden Codex app-server now waits for terminal turn events before reading final text; previous `019f2de5...` test returned the first assistant "checking files" message because the hidden server was disposed too early.
- `delegate_subagent` MCP results are rendered as `subagent` activity, hidden from the main sidebar, and auto-opened in the right `subagent:<id>` tab so the child chat can be viewed like existing spawned subagents.
- Existing cached `delegate_subagent 실행` MCP cards are normalized on `history-cache` merge, so older threads such as `019f2de5...` can render as subagent activity without manually deleting cache.
- `019f2e0b...` was verified: parent turn completed and child `019f2e0c...` ran `deepseek/deepseek-v4-pro`. Subagent tabs now preserve that provider/model metadata and lock the picker to the spawned model, while normal side-chat pickers remain editable.
- Post-v0.1.39 local fix: chat history now strips Ask-user/continuation internal text, tracks visible timeline owner thread in the renderer, and avoids merging cached foreign user bubbles into native Codex rollout history.
- Delegate subagent hidden Codex turns now use `danger-full-access` and a no-`apply_patch` execution note while preserving dynamic caller-supplied provider/model routing.
- Electron app was manually verified: runtime connection, thread creation, and a real `gpt-5.4` turn returned `clean timeline`.
- Electron app was manually verified: app-server returned existing workspace threads and resumed a selected thread.
- External proxy operation is not the desired end-user architecture; protocol adapters should live inside `devil-codex` where feasible.
- Do not intentionally redesign or simplify Codex behaviors: follow Codex's GUI, features, workflows, and performance characteristics as closely as practical, except where multi-provider support or unavailable proprietary implementation requires a compatible reimplementation.
- Local toolchain currently has Node.js `v26.0.0`, npm `11.12.1`, and Git; Rust, Cargo, pnpm, and Bun are not installed.
- `.env` files are ignored. A safe presence check detected a non-empty `OPENAI_API_KEY`; do not inspect or commit `.env.local`.

## Change Log

See `.memoc/worklog/` and generated `.memoc/activity.md`.

### 2026-07-13 Unreal MCP reconnect relay

- Added a project-neutral virtual-session relay with default `3001 -> 3000/mcp` and loopback-only environment overrides. Stale upstream sessions are recreated internally; only `tools/list` retries once. `tools/call` returns 503 and is never replayed.
- Desktop startup and the background Stock Bridge service start the relay. Main TypeScript compile and a live Unreal Editor restart/reconnect check passed.
- Relay forwarding now consumes aborted/error upstream streams and closes the downstream response safely, preventing an Unreal restart from becoming an unhandled Electron main-process exception. `npm run test:main` covers this interruption path.

### 2026-07-13 GPT-5.6 runtime support

- Bundled Codex app-server release target is `rust-v0.144.3`; its live `model/list` returns `gpt-5.6-sol`, `gpt-5.6-terra`, and `gpt-5.6-luna`. Devil now exposes only models returned by the active app-server, preventing a stale model cache from showing IDs rejected at `turn/start`.

### 2026-07-16 GPT-5.6 native picker recovery

- User's installed Devil Codex `0.2.16` bundles Codex `0.144.3`; direct authenticated `model/list` and the native cache both contain Sol/Terra/Luna, but the picker could remain on its initial static list when discovery raced startup. v0.2.17 merges only visible native cache entries (no `:`/`/` proxy IDs) with live `model/list`, and the renderer retries one empty/failed startup lookup after two seconds. Native Codex turns remain direct. `npm run build`, `npm run test:main`, and a local `listModels()` smoke all pass; package/release plus installed-app click-through remain pending.
- Follow-up screenshot from installed v0.2.17 proved the UI still used `provider-settings.cts`'s static Codex fallback, which had only GPT-5.5/5.4/5.4 Mini. v0.2.18 adds `gpt-5.6-sol`, `gpt-5.6-terra`, and `gpt-5.6-luna` to that direct-Codex fallback. It is deliberately not applied to OpenAI API-key or external proxy providers.

### 2026-07-16 Bridge model picker UX

- v0.2.19 replaces Settings -> 구성 -> Bridge's flat external-model list with the same hierarchy as the Composer picker: Provider -> account -> model. It adds provider/account/model text search, toggles selected models into or out of `stockBridgeModels` immediately, and retains selected-model ordering/removal below the chooser. This affects only the Stock Bridge selection UI; native Codex models remain direct and selected external rows remain proxy-routed. `npm run build` passes; test, commit/tag/push and installed-app click-through remain pending.
